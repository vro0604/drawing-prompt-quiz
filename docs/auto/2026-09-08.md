# 2026-09-08 無人実行の記録

## 1. 今日やったこと

絵1枚を、どれだけの大きさの箱に、どう収めているかを外から調べ、調査レポートを
1本書いた。docs/RESEARCH/2026-09-08_絵の収め方.md 。

集めたのは8件。CSS の object-fit の定義（MDN の原文）、Discourse（掲示板）、
Lemmy（掲示板）、Mastodon、Flickr の justified-layout 、Immich 、Bootstrap 、
Excalidraw 。8件すべて、公開リポジトリを複製して原本のファイルを直接読んだ。
読んだ版の commit と日付を各事例に書いた。

あわせて、いまの実装が絵をどう扱っているかを CSS の指定から計算した。
一覧のカードは絵の箱の高さが 192px に固定されていて、はみ出しは切り取られる。
1024px 以上の画面で、縦長（2:3）の絵は 53.5% が一覧に出ていない。
逆に作品ページの絵には高さの上限が無く、携帯で縦長の絵を開くと絵だけで 513px になる。

末尾に判断が要る点を3件置いた。A が一覧で切り取りをやめるか、
B が作品ページの絵に高さの上限を付けるか、C が絵の後ろと縁をどう作るか。

コードは一行も変えていない。書いたのは docs/RESEARCH/ と docs/auto/ の2か所だけ。

### 起動時に確認したこと

CLAUDE.md の「起動したら最初に確認すること」に従って、本流へ取り込まれていない
作業線を先に見た。

- `git branch -r | grep auto/` では何も出ない。複製が main しか持っていないため。
  GitHub の側で枝の一覧と開いている PR の一覧を見て確かめ直した。
- auto/ から始まる調査の枝は3本。2026-08-31（主色）と 2026-09-02（当たり外れ）は
  すでに main に取り込まれている（2d53173 と 3a45c8b の merge）。
- 取り込まれていないのは auto/research-2026-09-04（PR #8 、絵を見せたまま選択肢を
  出す画面の組み方、事例9件）の1本だけ。
- 今日の題材はこれと重ならない。向こうは選択肢の側の寸法と並べ方で、
  こちらは絵そのものの寸法。ただし1点だけ同じ場所に出る。向こうは
  「選択肢が画面に収まらない」を選択肢の高さの側から見つけていて、
  こちらは同じことを絵の高さの側から見つけた。レポートの中でそのことを書き、
  判断が要る点 B の選択肢に PR #8 の推奨を並べて、競合しないことを添えた。
- PR #2 、#5 、#6 、#7 も開いたままだが、#6 と #7 の中身は main に入っている。
  片付けは人の側の操作なので触っていない。

### 候補（docs/PROPOSALS.md）は足していない

同ファイルの規則が「未選択（`[ ]`）の候補が1つでも残っているあいだは足さない」と
定めていて、P1 から P4 の4件すべてが未選択のまま main に載っているため。
これは 2026-08-31 と 2026-09-02 の回と同じ判断で、3回続けて同じ理由で足せていない。

## 2. 注文書のどの項目を終えたか

docs/QUEUE.md の項目には着手していない。このリポジトリの無人実行は
CLAUDE.md の規約で調査専用と決まっていて、QUEUE.md に並んでいるのは実装の項目のため。

規約の「1回の起動でやること」に対しては次のとおり。

- 手順0（候補を出すか）: 出していない。未選択の候補が4件残っているため。
- 手順1（題材を決める）: 採用済み（`[x]`）で未実施の候補は無かったので、自分で選んだ。
  docs/RESEARCH/README.md の「題材の範囲」にある「絵を主役にした配色と余白の取り方」に
  当たる題材で、既存の調査3本がどれも絵そのものの寸法を扱っていないため選んだ。
  docs/PENDING.md で決まっている見た目の方向（紙・掲示板寄り、明るい側に固定）の中で、
  判断材料が無いところにあたる。
- 手順2から4: 実施した。事例は8件で、規約の下限（5件）と上限（10件）の中に入っている。

## 3. 検証の結果

- ページを開いて読む道具（WebFetch）は使えなかった。試したのは
  design-system.service.gov.uk と www.w3.org の2件で、どちらも
  `EGRESS_BLOCKED`（組織の通信方針による拒否）。迂回はしていない。
  ウェブページとして開いたものは1件も無く、そのことをレポートの冒頭に書いた。
- 検索（WebSearch）は通った。ただし返ってきた日本語の記事は数値の出所が
  たどれない一般論だったので、事例には1件も採っていない。
- 公開リポジトリの複製は通った。今日読んだのは8つ。
  mdn/content（44eeecc）、discourse/discourse（d82756af）、
  LemmyNet/lemmy-ui（c277a8a）、mastodon/mastodon（a6d857a）、
  flickr/justified-layout（9d4db49）、immich-app/immich（58fb1ed）、
  twbs/bootstrap（12cb8b9）、excalidraw/excalidraw（0361bdd）。
  複製先はすべて作業用の一時領域で、リポジトリの中には置いていない。
  大きいものは `--filter=blob:none --sparse` で必要な場所だけ取り出した。
- 寸法は自分で計算した。もとにしたのは src/app/works/(list)/page.tsx 、
  src/app/works/[id]/page.tsx 、src/app/_surface.ts の指定と、Tailwind の
  既定値（max-w-5xl = 1024px 、max-w-3xl = 768px 、gap-5 = 20px 、p-3 = 12px 、
  p-6 = 24px 、p-10 = 40px 、h-48 = 192px）。
  切り取られる割合は object-fit: cover の定義どおりに計算した。
- 変更範囲は `git status` で確かめた。docs/RESEARCH/ と docs/auto/ の下だけ。

## 4. 次に来る人が知っておくべきこと

- この環境ではページを直接開けないが、公開リポジトリは複製して読める。
  2026-09-02 の回と同じ結果で、3回続けて同じ制約が出ている。大きいリポジトリでも
  `--depth 1 --filter=blob:none --sparse` を使えば必要な場所だけ取れる。
  MDN の記事は `files/en-us/web/css/reference/properties/<プロパティ名>/index.md` に
  移っていて、以前の `files/en-us/web/css/<プロパティ名>/` には無い。
- 調査は今日で4本になった。主色（色）、当たり外れ（結果の見せ方）、
  選択肢の画面（答える前の並び）、絵の収め方（絵の寸法）。
  次に空いているのは、docs/RESEARCH/README.md の範囲のうち
  「限定公開の運用」と「投稿と閲覧が混ざるサービスの画面の組み方」のうち
  投稿の側（作品を出すまでの流れ）。
- 判断が要る点は、4本で合計12件たまった。返事が無いあいだは、次の調査が
  前の調査の推奨を前提に積み上がっていく。今日の C は 2026-08-31 の
  「地を紙色にする」推奨の上に乗っている。前提が変わると2本ぶんが組み直しになる。
- docs/PROPOSALS.md の P1 から P4 は、無人実行が調査専用になる前に出された
  実装の候補で、3回続けて「未選択が残っているので足さない」の理由になっている。
  調査の候補を出させたいなら、4件を `[x]` か `[-]` で片付けるか、
  実装候補と調査候補を別の節に分ける必要がある。2026-09-02 の記録にも同じことが
  書いてあり、状況は変わっていない。
