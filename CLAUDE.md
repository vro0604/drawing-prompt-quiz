@AGENTS.md

---

# 無人実行（クラウドのルーチン）の規約

平日の日中、ユーザーが不在のあいだにクラウド上の Claude Code がこのリポジトリを
クローンして作業する。以下はそのときだけ適用される規約。人が手で作業するときは
無視してよい。

## 絶対に守ること

1. main ブランチに直接コミットしない。必ず `auto/YYYY-MM-DD-短い説明` の形で
   新しいブランチを切り、そこで作業して PR を出す。ユーザーが PR を閉じれば
   作業が無かったことになる、という状態を常に保つ。
2. 検証を通してから PR を出す。下の検証コマンドが1つでも失敗したら PR を作らず、
   何がどう失敗したかを報告して終わる。壊れたものを PR にしない。
3. 止まる条件を、次の3つだけに限る。ここに当たらないものは止まらずに直す。
   - 製品として何を作るかが変わるとき（仕様の変更）
   - 何を正しいとするかの定義そのものが変わるとき（Canonical 定義の変更）
   - どの資料を優先するかが変わるとき

   この3つに当たるものだけを docs/PENDING.md へ書く。書式は
   「番号 / 何を決めたいか / 選択肢 / 私の推奨と理由 / そのまま返せる短い答え方」。

   逆に、次のものは判断待ちへ回さない。既存の方針で機械的に直せるので、自分で直して
   何を直したかを報告する。
   - 検査で見つかった不具合。1件見つけるたびに止まらない。
   - 書いてあることと実態の食い違い。実態に合わせるのが既定。
   - 同じ値が複数箇所にあって食い違っているものの一本化。
   - 名前・書式・置き場所のような、決め直しても手戻りが小さいもの。

   出所: ユーザー発言「自動作業は『新しい検査問題を1件見つけるたびに停止して判断を
   求める』のではなく、既存方針で機械的に直せるものは自律的に修正し、製品仕様・
   Canonical定義・資料の優先順位が変わる場合だけ停止すること」（2026-08-28）。
4. 外部に出る操作をしない。メール送信、SNS 投稿、本番環境へのデプロイ、
   外部 API への書き込みは行わない。
5. 削除は最小限にする。ファイルを消す必要が出たら、消さずに PENDING.md へ回す。

## 検証コマンド

作業のあと、必ず次を順に実行する。

```
npm ci
npm run typecheck
npm run lint
```

## 1回の起動でやること

0. docs/PROPOSALS.md を読む。未選択（`[ ]`）の候補が1つでも残っていれば、この手順は
   飛ばして 1 へ進む。1つも残っていなければ、このプロジェクトで1回1時間から2時間かかる
   作業の候補を3つから4つ挙げ、同ファイルの「候補」節に書式どおり書く。挙げるだけで
   着手はしない。見積もりには根拠を1行付ける。触るファイルの数や行数など、数えられる
   もので書くこと。候補を書いたら、そのまま 1 へ進む（候補出しで1回の起動を終えない）。
1. docs/QUEUE.md を読む。未着手の項目があれば、先頭の1つを実行する。
   QUEUE.md 自体は書き換えない。読むだけ。終えた項目の完了印は、
   docs/auto/<今日の日付>.md の側に「終えた項目」として見出しを引いて書く。
2. QUEUE.md が空なら自律モードに入る。順に `/next` `/map` で現在地を確認し、
   `/pending` で未回答の判断を洗い出す。ただし docs/PENDING.md へ書くのは、
   上の第3条の3つ（仕様の変更・何を正しいとするかの定義の変更・資料の優先順位の
   変更）に当たるものだけ。それ以外は判断待ちに積まず、次の `/chores` で直す側へ回す。
   そのうえで、
   `/chores` で雑務を洗い出してそのまま直す。
3. その日の記録を docs/auto/<今日の日付>.md に新しく書く。PROGRESS.md は書き換えない。
   PROGRESS.md はユーザー専用で、無人実行は読むだけ。両方が同じファイルの上に足すと
   取り込むときにぶつかるため（2026-08-28 の夜に実際に3回起きた）。
   書く内容は docs/auto/README.md にある4項目。
4. 検証コマンドを実行する。通ったら PR を作る。通らなければ PR を作らない。
5. 最後に結果を1行でプッシュ通知する。成功したか失敗したか、何をしたかが
   1行で分かるように書く。

## 変更の量

1回の PR は、ユーザーが夜に15分で読める量に収める。大きくなりそうなら
途中で切り上げ、残りを docs/auto/<今日の日付>.md に「次回へ回す分」として書く。
QUEUE.md には書き足さない。
