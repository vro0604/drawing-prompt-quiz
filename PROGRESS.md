# PROGRESS

セッションの引き継ぎを置く場所です。新しいものが上。
`/next` はここを起点に「現在地と次の一手」を出し、`/progress` がここへ追記します。
日時は JST。全体が400行を超えたら、一番下（最も古いもの）から削ります。

---

## 2026-09-06（3回目）本番へ当てて、新しい画面を出した

### 終えたこと

- **未適用だった18本の migration を本番へ当てた。**16:37 開始・16:38 終了。
  18本すべてが順に当たり、失敗も再実行も無い。本番の履歴は40本になった。
  出た警告は「Docker が無いので差分カタログを作れない」の1つだけで、
  これは CLI の補助処理。適用そのものとは無関係。
- **当てる前に、戻せる控えを自分で作って、戻せることを実際に確かめた。**
  この端末には pg_dump も Docker も無いので、読むだけの接続で public スキーマ
  29表・44,765行を INSERT 文として書き出した（`backup/prod-preapply-20260906/`。
  Git 追跡外）。追跡済み22本だけを当てた空のDBへ流し込むと、**29表すべてで
  件数が本番と一致した。**「作った」ではなく「戻した」まで確かめてある。
- **本番の検証で1度だけ不合格が出たが、原因は検査側だった。**
  マスタ行数4項目（tag_pools・card_slots・draft_modes・有効タグ）の期待値が
  18本を当てる前のままだった。40本を当てた手元のDBで数え直すと本番と同じ
  17/40/4/474。期待値を直して再実行し、**182項目すべて合格**。
  この4項目は `db:verify:local` 側で判定を飛ばす対象に入っていたので、
  手元の検査では気づけない構造になっている（**その構造は直していない**）。
- **旧い画面が新しいDBで動くことを確かめてから、新しい画面を出した。**
  旧いコードのまま `/` `/play` `/works` ほか9経路が 200。作品一覧もモード一覧も
  落ちない。そのうえで push（16:46:34）し、90秒後に新コードが本番で見えた。
  **DB更新から新コードまでの空きは約10分**で、監査が挙げた「旧い画面で
  新しく引いたお題が期限切れで投稿を弾かれる」窓（45〜90分）の内側に収まった。
- **本番の中心動線を一周した**（`npm run smoke:prod -- cutover`。新規作成）。
  お題1・作品1・回答2・返歌1・保存枠2・匿名1人・登録2人だけを作って、
  **すべて期待どおり・不合格0**。2段抽選、制作タイマーと延長、フレーバー、
  全語クイズ（3問固定ではなく語数と同数）、ビタ当てと2択当て、
  ヒント使用の分離記録、返歌、自分／他者由来の保存枠、未回答フィルタ、
  共有とOGP、Storage の画像まで通った。
- **試験データを片づけた。**作品は画面から削除（論理削除・画像も削除済み）。
  ゲストの一時持ち出しの枠だけは画面に出ないので、IDと利用者とscopeの
  3条件を同時に満たす行だけを名指しで消した。**既存の889作品・648回答・
  96保存には触れていない**（saves は 96 のまま）。

### 引き換えにしたもの

- **本番に、削除済みの試験作品1件と回答2件とお題1件と利用者3人が残る。**
  行ごと消さないのは runbook と D63 の決まり（works は回答・いいね・通報から
  参照されている）。公開一覧にもランキングにも出ないことは実測で確かめた。
- **旧い画面のモード一覧の中身が変わった。**`easy` / `standard` ではなく
  `normal` / `hard` が出る。絞っているのが画面ではなく RLS のポリシー
  （`draft_modes_select_active`。元からあったもの）だったため。
  **監査文書の「表示は変わらない」という記述のほうが間違っていた。**
  行は消していないので戻すのは1文で足りる。壊れてはいない。
- 検査の期待値を4つ書き換えた。根拠は migration が定める姿の実測と
  `docs/prod-audit-2026-09-06.md` 4章。**本番に合わせて緩めたのではない。**

### まだ手つかず

- **Vercel 側が何も見えない。**`VERCEL_TOKEN` の権限不足（403）で、
  deployment ID もログもエラー監視も取れない。戻すときは管理画面から
  「1つ前のデプロイ」を選ぶ形になる。
- **Cron を手で叩けない。**手元の `CRON_SECRET` が Vercel の値と違う（401）。
  掃除の対象は9項目すべて0件なので、いま実害は無い。
  古い active お題104件はどの対象にも入っていない（期限を遡及しなかったため）。
- **メール確認の到達・Turnstile の送信・カラーが出た画面・D169 の循環**は
  本番で見ていない。前2つは実機が要る。カラーは抽選で引かれなかった（9分の1）。
  循環は公開作品が0件で候補データ不足。
- 互換ラッパーと旧列は**消していない**。別の日の別の判断（runbook 3）。
- 課金・noindex解除・独自ドメイン・D118・D165の未確定値は、どれも触っていない。

### 触ったファイル

- `docs/verify-2026-09-06.md`（新規。今回の記録の正本）
- `scripts/smoke-cutover.mjs`（新規。本番切替後の一周）
- `scripts/db-checks.mjs`（マスタ行数4項目の期待値）
- `PROGRESS.md`
- 保存点 `9f878f2` ／ 検証修正 `bf440f3`

---

## 2026-09-06（2回目）本番へ当てる前に、旧い画面が壊れない形へ直した

### 終えたこと

- **旧い画面と新しいDBが同時に動く時間帯で、壊れる箇所を実測で見つけた。**
  本番は「DBを先に更新し、そのあと画面を差し替える」順で当てる。
  その間、いま Vercel で動いているコードと新しいDBが組み合わさる。
  推測ではなく、PGlite で2つのDBを実際に作って突き合わせた
  （`npm run audit:schema-diff`）。壊れるのは3か所だった。
  作品一覧（`get_public_works` の5引数版を drop していた）、
  モード選択（`draft_modes.quiz_question_count` を drop していた）、
  二重送信からの合流（`draft_state_json` の鍵が消えていた）。
- **その3か所を、旧い形の入口を残す形へ直した。**
  旧5引数版と旧2引数版は、新版を呼ぶだけの入口として残す。
  旧列は落とさず、読み取り権限も外さない。旧い鍵も返し続ける。
  削除は新しい画面の本番稼働を確かめた後の別 migration にする。
- **並存が曖昧にならない形を、実測で決めた。**
  6引数版の6番目に既定値を付けたまま5引数版と並べると、5引数の呼び出しが
  両方に当てはまり `function is not unique` で**新旧どちらも落ちる。**
  Postgres は「既定値のある引数の後ろに既定値の無い引数」も許さない。
  そこで新6引数版は6つとも既定値を持たない形にした。
  引数5個以下は旧版、6個は新版。migration の中で5通り・6通り実際に呼んで確かめる。
- **本番を読むだけで事前監査した**（`npm run db:audit:prod`）。
  `begin transaction read only` の中で走らせ、rollback で閉じる。
  1文字も書いていない。未適用は**実測で18本**（本番22本／手元40本／欠番0）。
  当てる前の当たり判定12件はすべて通った。
- **当てた瞬間に走る文を全件数えた**（`npm run audit:migration-ops`）。
  `DELETE` も `TRUNCATE` も `DROP COLUMN` も0本。`DROP TABLE` は一時控え1本だけ。
- **手順と戻し方を実体にした。**適用27段と、失敗地点ごとの戻し方12通り。
  復旧SQLは別ファイルに置いた（**未実行**）。

### 引き換えにしたもの

- 旧い入口が2つ増えたぶん、公開されている関数の数が増えた。
  削除の判断が1回、後ろに残る。
- **互換化しきれなかったものが1つある。**制作の期限のガードが、
  タイマーを持たない旧い画面に当たる。DB更新後に新しく引かれたお題に限り、
  期限＋猶予（1.5T）を過ぎてからの投稿が拒まれる。消すには機能を止めるしかない。
  対処は「DB更新とデプロイの間を空けない」こと。

### どこまでで、どこからが未着手か

**触ったのは、手元のファイルと文書だけ。**本番DBへの書き込み、migration の適用、
Vercel へのデプロイ、コミット、課金は1つも行っていない。
既存お題への期限の遡及も、D118 の判断も、D165 の未確定値も動かしていない。

### 次の一手

**本番へ当てる。**許可が出たら `docs/prod-runbook.md` の27段を上から通す。

### 触ったファイル

- supabase/migrations/20260905095000_d165_all_words_quiz.sql（列削除をやめた）
- supabase/migrations/20260906090000_d169_next_work_division_and_unanswered.sql
  （旧版を drop せず互換ラッパーにした）
- supabase/rollback/2026-09-06_cutover_recovery.sql（新規・未実行）
- scripts/db-audit-prod.mjs / db-audit-prod.sh / scan-migration-ops.mjs（新規）
- scripts/db-checks.mjs（互換の検査7件と役の試験3件を追加）
- test/audit/schema-diff.mjs（新規）
- test/db/run.mjs（互換4件）・test/db/upgrade.mjs（互換3件）
- docs/prod-audit-2026-09-06.md / docs/prod-runbook.md（新規・どちらも正本）
- docs/decisions.md / spec.md / GPT_HANDOFF.md / launch-checklist.md /
  launch-run.md / test-layers.md / verify-2026-09-05.md /
  postlaunch-feature-architecture.md / README.md / PROGRESS.md
- package.json（db:audit:prod / audit:schema-diff / audit:migration-ops）

---

## 2026-09-06（未明）試験の間欠失敗を直し、D169 の3項目を実装した

### 終えたこと

- **ブラウザ試験が「まれに1件だけ落ちる」原因を突き止めて直した。**
  原因は2つあり、**どちらも検査の側**にあった。アプリの不具合ではない。
  1つめは、送信のあとに画面を読むのが早すぎたこと。このサービスの送信は
  応答が返ったあとに**もう一度サーバーから画面を取り直す**作りで、
  その2回目が届く前に読むと、前の画面を見て「出ていない」と判定していた。
  2つめは、`next dev` が画面を組み立てる数秒が試験の待ち時間を食っていたこと。
  組み立ては試験の外へ出した（先に1度ずつ開いて温める）。
  **試験の中の待ち時間は1ミリ秒も伸ばしていない。**
- **落ちた1件が何かを、記録に残るようにした。**前回はそれが分からなかった。
  合格・不合格を問わず全件ぶんを `.test-logs/` へ書く。1件ごとに
  試験名・開始と終了の時刻・落ちた段・そのときのURL・内側の例外・
  落ちかたの種類（プロセス終了／メモリ不足／ポート拒否／通信断／画面待ち／
  要素待ち／判定の不一致の7つ）。**この記録のおかげで、上の原因が分かった。**
- **重い検査が2つ同時に走れないようにした。**注意書きではなく札で止める。
  ブラウザ試験・スモーク・DB試験2本・DB構造の検査は自分で札を取り、
  build は一括の検査の側で札を取る。別の窓から叩いても順番待ちになる。
- **記録の仕掛けそのものを試す自己試験を足した**（`npm run test:e2e:selftest`）。
  本物のブラウザ試験をわざと1件落として、名前と原因が残ることを確かめる。
  札が2つ同時に取れないことも、ここで見る。
- **D169 のうち初期公開向けの3項目を実装した。**
  1. 次の作品は、いま答えた作品と**同じ部門**から出る。部門は引数で渡せない
     形にした（`get_next_work` から部門の引数を消した）。DB が現在作品の行から読む。
  2. **回答0件の作品を最優先。**あればその中だけから無作為に1件。無ければ
     回答1件以上から無作為に1件。**帯の中では回答数の順位を付けない。**
  3. `/works` に「未回答のみ」を足した。部門・完成度・並び順・ページと併用できる。
     見るのは**回答を送ったかどうかだけ**で、開いただけでは消えない。
- **文書の件数が古くならないようにした。**数のうしろに見えない印を付け、
  `npm run check:docs` がその回の実測と突き合わせる。合わなければ test:all が落ちる。

### 検査の結果（この端末で実測）

- `npm run test:e2e` … **単独で5回連続 51/51**（40件から11件増えた）
- `npm run test:all` … **3回連続で全13工程 合格**
- 柵30 / 縦断115 / アップグレード24 / DB構造163 / 語彙564 / スモーク11 / 記録の自己試験21
- TypeScript・ESLint・配色・build … いずれも成功

### 引き換えにしたもの

- **ブラウザ試験が長くなった。**約100秒から約140秒。画面を先に温めるぶんと、
  本文が届くのを待つぶん。**待ちの上限は決めてあり、出ないものは落ちる。**
- **`test:all` が1工程増えた**（記録の自己試験）。ブラウザ試験を2回まわす形に
  なるので、1周が約3分長い。落ちたときに原因が残ることを毎回確かめるための費用。
- **本番へ当てる前に、画面とDBの食い違いが一瞬だけ起きる。**
  `get_next_work` と `get_public_works` の引数の数が変わったので、
  古い画面と新しいDB（またはその逆）が混ざる間は、次の作品と一覧が失敗する。
  **DB を先、画面をあと**の順で当てる（docs/launch-checklist.md の手順 -1）。

### 次の一手

**本番へ当てる。**未適用の migration が18本（D169 のぶん1本を含む）。
`npm run db:deploy` → `npm run db:verify:keychain` → Vercel。課金には進まない。

**D118 は未確定のまま残っている。**「一覧で見た作品を回答対象から外すか」（案a）を
決めていない。いまの「未回答のみ」は回答履歴だけを見ており、開いただけでは
消えない。この振る舞いは縦断試験とブラウザ試験で固定してあるので、
案a を採るとその2件が落ちる。

### 触ったファイル

- supabase/migrations/20260906090000_d169_next_work_division_and_unanswered.sql（新規）
- src/features/discover/rpc.ts ／ src/features/work/rpc.ts
- src/app/works/(list)/page.tsx ／ src/app/works/[id]/_after.tsx
- test/e2e/exclusive.mjs ／ record.mjs ／ record-selftest.mjs ／ _lock-probe.mjs（新規）
- test/counts.mjs（新規）／ scripts/heavy.mjs ／ scripts/check-doc-counts.mjs（新規）
- test/e2e/browser.mjs ／ server.mjs ／ smoke.mjs ／ seed.mjs
- test/db/run.mjs ／ upgrade.mjs ／ helpers.mjs ／ test/guard/selftest.mjs
- scripts/run-all-checks.mjs ／ db-checks.mjs ／ db-verify-local.mjs ／ vocab-audit.mjs
- package.json ／ .gitignore
- docs/decisions.md ／ spec.md ／ GPT_HANDOFF.md ／ verify-2026-09-05.md ／
  test-layers.md ／ launch-checklist.md ／ README.md ／ PROGRESS.md

**本番DBへの適用・デプロイ・課金・コミットは、いずれも行っていない。**

---

## 2026-09-06（深夜）消えた報告の復元と、残っていた取りこぼしの始末

前回の応答が表示前に消えたため、作業ツリー・差分・文書・実測から状態を復元した。
**やり直しはしていない。**2026-09-05（3回目）の作業はすべて残っていた。
そのうえで、そこで取りこぼしていた3つを直した。

### 終えたこと

- **試験を回し直して、記録どおりに通ることを確かめた。**
  柵30、縦断100、アップグレード22、DB構造163、語彙564語の三者一致、
  スモーク11本×5周＝55、smoke-anon 単独10回。**記録は正しかった。**
  ただしブラウザ試験だけは、単独4回のうち1回が 39/40 で落ちた（下記）。
- **控えファイルが Git から漏れる状態だったのを塞いだ。**
  スモークを複数周まわすと `.smoke-fixtures.e2e.2.json` から `.10.json` まで
  増えるが、除外していたのは番号の無い1本だけだった。Cookie と合言葉が
  入るファイルが9本、追跡されないまま置かれていた。番号付きもまとめて外した。
- **ブラウザ試験が他の検査と同時に走ると落ちることが分かった。**
  同時に回して 4/40（この端末で実測）。画面が出るまでの待ち時間を15秒で切る
  作りなので、計算機の取り合いで越える。アプリの不具合ではない。
  README と検証記録の両方に「同時に回さない」と書いた。

- **未解決。ブラウザ試験は単独でも、まれに1件だけ落ちる。**
  単独で4回回して 40/40 が3回、39/40 が1回。落ちた1件がどれかは、
  そのとき出力の末尾3行しか残していなかったので分かっていない。
  **再現条件も原因も未特定。**次に回すときは全文を残すこと。
- **古くなっていた件数を実測値へ直した。**README の migration 30本（実際は39本）、
  縦断63（100）、アップグレード15（22）、ブラウザ34（40）。検証記録も同じく直した。
- **未適用の migration の本数を数え直した。**18本と書いていたが17本。

### 引き換えにしたもの

引き換えは無し。**コードは1行も変えていない。**直したのは除外設定と文書だけ。

### 次の一手

前回から変わらない。**本番へ当てる。**未適用の migration が17本。
`npm run db:deploy` → `npm run db:verify:keychain` → Vercel。課金には進まない。

### 触ったファイル

- .gitignore（控えファイルの除外を番号付きへ広げた）
- README.md（件数の訂正、柵の行の追加、同時実行の注意）
- docs/verify-2026-09-05.md（第2節の件数を実測値へ）
- PROGRESS.md

---

## 2026-09-06 仕様確定（D169）次の作品の配給とパーソナライズ

### 終えたこと

- **「次の作品に答える」の選定方針をユーザーが確定し、決定記録へ D169 として置いた。**
  実装はしていない。コード・マイグレーション・本番DBには一切触っていない。
  中身は、この導線を推薦ではなく「回答不足作品へ回答者を割り当てる配給」として扱い、
  回答不足救済を個人適合より上位の目的とすること。救済は単一スコアの係数ではなく、
  候補生成の段の硬い制約として置き、個人適合度は救済帯をまたいで作品を押し上げない。
- 初期公開の救済帯は2つだけ（回答0件／1件以上）。目標回答数Tは実測が無いので決めない。
  帯の中は当面ランダム。初期公開ではパーソナライズしない。
- 正答率・カテゴリ別正答率・ビタ当て率・2択利用率を「好み」の推定に使わないことを明記した。
  正解しやすい作品を優先すると、伝達率が「不特定の回答者にどこまで伝わったか」ではなく
  「伝わりやすい人を選んで見せた結果」に変質するため。
- AI部門の次作品遷移は「現在の部門を引き継ぐ」に確定。いまのコードは満たしていない
  （src/features/discover/rpc.ts が部門の指定なしで固定して呼んでいる）。直していない。
- **番号を間違えて D166 で書き、D169 に直した。**別セッションが 2026-09-05 に
  D166・D167・D168 を使っていた。追記する前に末尾の番号を確認していなかった。
- **競合する記述を4文書7箇所に見つけ、すべてに D169 への参照を入れた。**
  いちばん重いのは D118（回答すると同じ札の作品が開く）で、
  その案a「一覧の作品は鑑賞のみにする」が回答対象を減らすため、
  回答不足救済と正面からぶつかる。**どちらを採るかは未確定として残した。**
- 統合.md が同時刻に更新されているが、私が書いたものではない。
  文書を直したことで自動で作り直されたもの。

### 次の一手

**D118 の扱いが未確定のまま残っている。** ユーザーの 2026-09-06 の確定に
D118 は含まれていない。D118 を実装するとき、案a（見た作品を鑑賞のみにして
回答対象から外す）を採るかどうかを決める必要がある。D127 自身が
「限定公開の数件では回答先が即座に枯れる」と書いている。

実装として残っているのは3つ。どれも今回は手を付けていない。

1. 次の作品で部門を引き継ぐ（D169 の12）
2. 救済帯を0回答とそれ以外の2つに割る（D169 の3）
3. 一覧に「未回答のみ」の絞り込みを足す（D169 の11）

問題数フィルタの前提（全語出題）は 2026-09-05 に実装済み。ただし既存作品の出題を
作り直していないので、旧方式で3問のまま残る作品がある。フィルタを作るなら扱いを決める。

### 触ったファイル

- docs/decisions.md（D169 を追加。D118 と D127 に追記）
- docs/audit-2026-09-04.md（A5-1 の判定を取り下げ、A5-2 と実行順に注記）
- docs/GPT_HANDOFF.md（優先候補の D118 に、次の作品とは別物だと注記）
- docs/postlaunch-feature-architecture.md（3-3 に、作品open履歴は不要と確定した
  わけではないと注記）
- PROGRESS.md

**コード・マイグレーション・検査・本番DB・コミットは、いずれも行っていない。**

