# PROGRESS

セッションの引き継ぎを置く場所です。新しいものが上。
`/next` はここを起点に「現在地と次の一手」を出し、`/progress` がここへ追記します。
日時は JST。全体が400行を超えたら、一番下（最も古いもの）から削ります。

---
## 2026-09-10 サブ指令の改ざん経路を塞ぎ、24語×3候補の対応表を入れた（D193）

### 何ができるようになったか

お題に出た正式な語1つに、「それをどう描くか」の手がかりが1つ添うようになった。
24語（モーフ8・感情6・性質4・カラー6）に3候補ずつ、合計72候補。
付くのは70%で、30%は何も付かない。候補の無い語には付かない。

```
恐怖
  └ 背後を気にしている
```

### 何を引き換えにしたか

カードを決める操作が、DBへの往復2回になった（決める／鍵を書く）。
後半だけ失敗すると、正式なお題は成立してサブ指令だけ付かない。
失敗はサーバーの記録へ残す。

表は52のまま。列2つと関数1本を足しただけ。

### 見つけて塞いだ穴

最初の作りは、鍵をカードを決める窓口の4つめの引数として渡していた。
あの窓口はサインイン済みなら誰でも直接叩ける。しかもサインインの証明書は
ブラウザから読み取れる（@supabase/ssr の既定が httpOnly: false。実測）。
だから画面を通さず、形さえ合っていれば好きな鍵を書き込めた。

直し方は「呼べる相手そのものを変える」。
- choose_card からサブ指令を丸ごと外し、元の3引数へ戻した（migration は触らない）
- 鍵を書く窓口を別に作り、PUBLIC/anon/authenticated から権限を取り上げ、
  サーバーだけが持つ鍵（service_role）にだけ配った
- その窓口の中で、誰の・どのドラフトの・どの世代の・どの枠の・どの語かを
  5つとも突き合わせる。引き直しが割り込んだら0行のまま静かに終わる

回帰試験として、偽の鍵（fake_modifier）を利用者が書けないことを3経路で見ている。

### 検査の実測

- 縦断試験 261/261（S2群21件・S3群9件を含む）
- DB構造の検査 197/197（A48・A49 を追加）
- 型検査 0、lint エラー0・警告2（すべて既存）
- ブラウザ試験は全件を流している最中

### 次の一手

1. 全件ブラウザ試験の結果を確認する
2. 緑ならサブ指令だけの独立コミットを作る（push しない）
3. 本番反映は別指示まで行わない

### 触ったファイル

- `supabase/migrations/20260910140000_sub_directive.sql`（新規・本番未適用）
- `src/features/modifier/types.ts`（対応表72件と70/30）、`src/features/modifier/rpc.ts`（新規）
- `src/app/play/actions.ts`、`src/features/draft/rpc.ts`、`src/lib/supabase/admin.ts`
- `src/app/play/_components.tsx`、`src/app/prompt/[id]/page.tsx`
- `scripts/db-checks.mjs`、`test/db/run.mjs`、`test/e2e/browser.mjs`
- `docs/decisions.md`（D193）、`docs/spec.md`（18節）

---
## 2026-09-10 回答の知らせを本番へ出した（D192。DB → push → 本番往復まで完了）

### 何ができるようになったか

自分の作品に回答が来たことを、作者が気付けるようになった。
ヘッダーに「知らせ」が出て、そこからまだ結果を見ていない作品の一覧へ行ける。
押すと、その作品の結果が開いた状態で開く。

これで「投稿する → 誰かが答える → 作者が知る → 結果を見る」が
一周つながった。これまでは作者が自分から作品ページへ戻らない限り、
回答が来たことに気付けなかった。

### 何を引き換えにしたか

引き換えが1つある。ヘッダーが状態を持つものになった。
これまでヘッダーは認証を1度も読んでいなかったが、
「未確認があるか」を全ページで1回聞くようになった。
聞くのは真偽値1つで、失敗しても知らせが出ないだけ（握りつぶす）。

DBは列が1つ増えた（`works.result_seen_at`）。表は増やしていない。
回答の側（submit_answer）は1行も変えていない。

### 表を増やさずに済ませた仕組み

知らせの行はどこにも保存していない。作品ごとに
「作者が最後に結果を開いた時刻」を1つ持ち、そこから後に来た回答があれば
未確認とする。**そのつど数えるので、知らせと実際の回答がずれない。**
回答を消せば知らせも消え、作品を消せば知らせも消える。
同期を取る処理が要らない。

### 数を運んでいない

ヘッダーが呼ぶ関数は真偽値しか返さない。一覧を返す関数にも件数の列が無い。
画面で消しているのではなく、**数そのものをDBから持ち出していない。**
D112（数字は取りに行った人にだけ出す）を保つため。
見た目の違いは文字が太くなるだけで、丸も数字も付けていない。

### 一覧を見ただけでは消えない

結果を実際に開くまで残る。そのために関数を2本に分けた。
作品ページを封を閉じたまま見たときは読むだけの関数を呼び、
`?result=open` のときだけ、時刻を書くほうの関数を呼ぶ。
結果画面そのものは既存のまま。新しい画面は作っていない。

### 手元の実測

- `npm run test:db` 231件すべて合格（T群15件を含む）
- `npm run db:verify:local` 合格192・不合格0（A42・A43・A44 を追加）
- `npm run typecheck` 終了コード0
- `npm run lint` エラー0・警告2（すべて既存）
- `npm run build` 成功（`/notices` が出た）
- `E2E_ONLY=T npm run test:e2e` T群7件すべて合格

### 本番へ出した（2026-09-10）

順番は migration → DB検査 → push → 反映 → 本番往復。

- `20260910090000_answer_notice.sql` を1本だけ当てた（他の49本には触れていない）。
  本番の migration は 49 → 50 本。
- 本番DB検査 203項目すべて合格・不合格0（A42・A43・A44 を足したので 200→203）。
- push は `2f3eb1c` の1本だけ。Vercel の反映は 45 秒後に確認。
- 本番往復（`npm run smoke:prod -- notice`）A〜I すべて合格。

**本番のデータは1件も壊れていない。**表52・タグ474 は前後で同じ。
既存の作品925件はすべて「まだ開いていない」から始まっている。

### 本番の検査を書くときに3回間違えた（どれも検査側）

- `get_art_first_vocabulary` の戻りを配列だと思っていた。
  実際は `{ max_words, min_words, categories: [{ kind, tags }] }`。
- 持ち込みの作品を秘密鍵で作ろうとして断られた（NOT_SIGNED_IN）。
  あの関数は auth.uid() で作者を決めるので、**本人の道を通す**必要がある。
- 選んだ語を同じ名前で3回送っていた。受け取る側はカンマで割って読むので、
  1件しか届かなかった。

### 次の一手

- 通知は本番完了。次は modifier（正式語ごとの自動制作アシスト）。
  **別の migration・別のコミットにする。**

### 触ったファイル

- `supabase/migrations/20260910090000_answer_notice.sql`（本番適用済み）
- `scripts/smoke-notice.mjs`（新規）、`package.json`
- `src/features/notice/{types.ts,rpc.ts}`（新規）、`src/app/notices/page.tsx`（新規）
- `src/features/work/rpc.ts`（openMyWorkResult を追加）
- `src/app/_shell.tsx`（ヘッダーに入口）、`src/app/works/[id]/page.tsx`
- `scripts/db-checks.mjs`（A42・A43・A44）
- `test/db/run.mjs`（T群15件）、`test/e2e/browser.mjs`（T群7件）
- `docs/decisions.md`（D192）、`docs/spec.md`（17節）、`PROGRESS.md`

### 400行を超えたので、いちばん古い1件を削った

削ったのは「2026-09-09 形状アシストを実装した（本番へはまだ当てていない）」。
**すでに本番へ出し終えている**（同じ日の「本番へ出した」の回が上にある）ので、
進行中の作業線の記録は1件も消していない。
中身は `docs/decisions.md` の D191 に残っている。

---
## 2026-09-09 形状アシストを本番へ出した（D191。DB → push → 本番往復まで完了）

### 何ができるようになったか

本番の利用者が、お題を引く前に「どういう形として描くか」の取っかかりを
1つだけ持てるようになった。使わない／ランダムに1つ／自分で選ぶ の3つ。
**正式なお題ではない。**守らなくても投稿できるし、クイズにも出ない。

### 何を引き換えにしたか

引き換えは無し。本番のデータは1件も動いていない
（作品919・お題1044・回答661・タグ474 は適用の前後で同じ）。表は52のまま。
既存のドラフト1021行はすべて空のままで、これまでのお題の動きは変わらない。

### 出した順序と、その理由

DBを先に当て、そのあと画面を出した。`start_draft` の引数が3つから4つへ
増えるので、逆順にすると反映までのあいだ、お題を引く操作が
引数の合わない呼び出しになって失敗する。実際にはDBが先だったので隙間は無い。

1. `20260909200000_shape_assist.sql` を `db:apply:one` で1本だけ当てた
   （他の48本には触れていない）。`migration repair` で履歴にも記録した。
2. 本番DB検査 200項目すべて合格・不合格0（A40・A41 を足したので198→200）。
3. `b765e40` `bce0f23` `2c1bf23` の3本を main へ早送りで push。
4. Vercel の反映を `/play` の `data-testid="shape-assist"` の有無で確認（約30秒）。
5. 本番往復（後述）。

### 本番往復は、本物のブラウザで押した

`npm run smoke:prod -- shape-assist` を新しく作った。Chromium を本番へ向け、
人が押すのと同じ順で押す。認証は既存の fixtureSession が取った Cookie を
ブラウザへ移して済ませている。24項目すべて合格。

作った作品2件は、検査の終わりに削除済み（本番に残っている公開作品は、
元からの本物2件だけ）。検査用の人の進行中ドラフトも0件に戻してある。

### 検査を書くときに3回間違えた（どれも機能ではなく検査側）

- 押した直後に次を読み、「決めています…」のまま進んで止まった。
  待ち時間を伸ばすのではなく、**終わりを示す印**（`aria-busy`）が
  消えるまで待つ形に直した。
- 作品の題名に「形状アシスト」と入れたせいで、漏れていないのに
  「本文にその語がある」と落ちた。題名を変えた。
- お題の語数を2つのお題で突き合わせていた。**語の顔ぶれは抽選で変わる**
  （実測で 4語 と 3語）ので比較が成り立たない。
  「そのお題の中で、出題数と出題対象の枠数が一致するか」に変えた。

### 次の一手

- 形状アシストは本番完了。次工程の指示待ち。
- 残件: Supabase の関数の既定 EXECUTE を手元の検査で再現する監査
  （プロフィールの4本と同じ漏れが他のRPCにも無いか。別タスク）。
- 残件: `docs/decisions.md` の D172 番号重複。

### 触ったファイル

- `supabase/migrations/20260909200000_shape_assist.sql`（本番適用済み）
- `scripts/smoke-shape-assist.mjs`（新規）、`package.json`
- `src/features/shape-assist/types.ts`、`src/features/draft/{types.ts,rpc.ts}`
- `src/app/play/{actions.ts,_components.tsx}`、`src/app/prompt/[id]/page.tsx`
- `scripts/db-checks.mjs`、`test/db/run.mjs`、`test/e2e/browser.mjs`
- `docs/{decisions.md,spec.md}`、`PROGRESS.md`

### 400行を超えたので、いちばん古い1件を削った

削ったのは「2026-09-09 管理 v0 を独立コミットにした」。
**すでに本番へ出し終えている**（同じ日のプロフィールの回が、その上に
載せた形で push 済み）ので、進行中の作業線の記録は1件も消していない。
中身は `docs/decisions.md` の D177 と `docs/verify-2026-09-09.md` に残っている。

---
---
## 2026-09-09 形状アシストと Color / Sub Color の仕様を先に固めて置いた（D189・D190。実装はしない）

### 終えたこと

- 形状アシスト（D189）。お題の中身ではなく「どういう形として出すか」の
  取っかかりだけを渡す。操作は「なし／ランダム／自分で選ぶ」。
  正式prompt ではなく、クイズ対象外、伝達結果の正式項目にもしない。
  守らなくても投稿できる。**正式名称と正式カテゴリ一覧は未確定。**
- Color を2つの役に分ける（D190）。Main Color は正式なお題でクイズ対象、
  原則1色。Sub Color は Main に合わせた制作参考色で、正式なお題ではなく
  クイズ対象外、守る必要もない。**Sub を正式prompt やクイズへ混ぜない。**
- どちらも旧資料に対応するものがあった。`docs/spec-candidates-2026-09-07.md`
  の候補B「表現フレーム」＝ 形状アシスト、候補H「補助色」＝ Sub Color。
  **呼びかたが変わっているので、旧資料を読むときは同じものとして読む。**

### 本番で測って分かったこと（作る日に効く）

- `card_slots` に `sub_color`（補助カラー）の枠が**すでにある。**
  ただし `is_quiz_eligible = true` で、今回の「Sub Color はクイズ対象外」と
  食い違う。実害は出ていない。`prompt_cards` を数えると `sub_color` は0件で、
  1度も使われていない（`main_color` は993件）。枠だけあって中身が無い状態。
  **作る日に、この行を false にするか `card_slots` から外すかを先に決める。**
  そのまま埋め始めると Sub Color がクイズに出る。いまは何も変えていない。
- 「原則1色」はいまも守られている。旧語彙は `main_color` が1枠、
  いまの語彙は `draw_categories.color` の `max_per_prompt` が 1。

### 400行を超えたので、いちばん古い1件を削った

削ったのは「2026-09-08（3回目）重い検査の排他を広げ、本番へ出す migration を
1本に確定した」。**どちらも終わっている。**重い検査の札の話は
`docs/test-layers.md` に、本番へ当てる前の控えは
`docs/prod-apply-2026-09-08.md` に移してある。art_first の migration は
2026-09-08 に本番へ当て終えた。進行中の作業線の記録は1件も消していない。

### 触ったファイル

- `docs/decisions.md`（D189・D190 を追記。既存の行は1つも消していない）
- `PROGRESS.md`

---
## 2026-09-09 検索の仕様を先に固めて置いた（D188。実装はしない）

### 終えたこと

- 検索システムの仕様を `docs/decisions.md` の D188 として書いた。
  **コードは1行も書いていない。**いまは作らない。着手の合図は
  「実利用上、検索の必要性が発生した段階」で、**件数の目安は決めない**
  （出所: ユーザー指示 2026-09-09「導入時期の数値条件はまだ決めない」）。
- 背骨は1行。「検索は、その利用者がその時点ですでに知ることを
  許可されている情報だけを索引として使用する」。
- 漏れる口は結果一覧だけではない。件数・autocomplete・suggestion・
  facet・filter件数・ranking・並び順のどれからも、まだ開示されていない
  クイズの正解を推測できてはならない。**「狐」と打って候補に出る、
  絞り込みの横に「(3)」と出る、それだけで答えが割れる。**
- 最初に作るのはマイページの回答履歴検索。対象はタイトル・作者・
  正式なお題タグ。正式タグを索引に載せてよいのは、自分の作品か、
  自分が回答済みの作品だけ。他人の未回答作品では、候補抽出・件数・
  ranking その他の検索処理に一切使わない。
- **画面で隠す方式は禁止。**DB/API 側で開示範囲を切る。

### 番号のとりかた（実測）

`docs/decisions.md` は共有の作業ツリーで D178〜D187 まで別作業線が
使っていた（2026-09-09 に実測）。空いている先頭が D188 だったのでそこに置いた。

### 400行を超えたので、いちばん古い1件を削った

削ったのは「2026-09-08（2回目）E2E の不合格4件を切り分け、いまの基準状態を
確定した」。**そこで決めた基準状態と E2E_ONLY の仕組みは、次の
「2026-09-08（3回目）」と `docs/test-layers.md` に引き継いである。**
進行中の作業線の記録は1件も消していない。

### 触ったファイル

- `docs/decisions.md`（D188 を追記。既存の行は1つも消していない）
- `PROGRESS.md`

---
