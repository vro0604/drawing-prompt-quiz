# PROGRESS

セッションの引き継ぎを置く場所です。新しいものが上。
`/next` はここを起点に「現在地と次の一手」を出し、`/progress` がここへ追記します。
日時は JST。全体が400行を超えたら、一番下（最も古いもの）から削ります。

---
## 2026-09-10 利用者を正規の経路で消せなかった原因を突き止めた

### 何ができるようになったか

本番の検査用利用者119人が、Supabase の管理API（正規の削除経路）で
必ず 500 になっていた。その原因が分かり、直し方も手元の PostgreSQL 17 で
実際に確かめた。**まだ本番へは当てていない（承認待ち）。**

原因は、引きかけのお題の「最後にさわった時刻」を書き直す引き金1つ。
利用者を消すと外部キーの後始末が
auth.users → profiles → draft_sessions → draft_candidates と連鎖する。
Postgres はこの後始末を「消される表の持ち主」の資格で走らせるが、
AFTER 行トリガーだけは文の終わりにまとめて実行されるため、そのときには
資格が呼び出し元（認証サービス）に戻っている。認証サービスは public の表に
権限を1つも持たないので、そこで draft_sessions を書こうとして断られる。

本番ログの実測（2026-09-10T14:36:46Z）:
`permission denied for table draft_sessions (SQLSTATE 42501)`。
消せた198人は draft_candidates を持っていなかった。残る119人は全員持っている。

### 何を引き換えにしたか

引き換えは無し。関数の中身は1行も変えず、実行資格だけを持ち主のものにする。
アプリの通常の経路は SECURITY DEFINER の RPC か service_role で動いていて、
anon と authenticated はこの2表に権限を1つも持たない（実測）ので、
見え方も動きも変わらない。

同じ形の見落としを二度としないために、DB構造の検査へ1項目足した。
「削除・更新で走る AFTER トリガーに、資格を切り替えないものが0本」。
いまの本番に当てると、この1本だけが引っかかる。

### どこまでで、どこからが未着手か

済み: 原因特定、migration の作成、PostgreSQL 17.5 での再現と検証、
DB構造の検査への項目追加、片づけ道具の失敗理由の出し方の作り直しと自己試験6件。
未着手: 本番への適用（承認待ち）と、そのあとの119人の片づけ。

### 次の一手

1. `20260910210000_touch_session_as_owner.sql` を本番へ当てる（`npm run db:deploy`。
   本番の最後より新しい版番号なので、履歴も自動で入る。repair は要らない）
2. `npm run db:verify:keychain` で新項目を含めて確認
3. `npm run smoke:prod -- fixtures`（下見）→ `--apply`（実行）→ 下見で0を確認

### 触ったファイル

- supabase/migrations/20260910210000_touch_session_as_owner.sql（新規）
- scripts/db-checks.mjs（AFTER トリガーの資格を見る項目を追加）
- scripts/_smoke-users.mjs（生のまま叩いて理由を読む／理由を種類ごとに数える／試験から差し替えられる口）
- scripts/smoke-fixtures.mjs（理由の出し方）
- test/tools/run.mjs（片づけ道具の自己試験6件）
- README.md / docs/test-layers.md / docs/launch-checklist.md（件数と、手順 -1 の現状）

---
## 2026-09-10 本番向けの検査を、新しい画面に追いつかせた

### 何ができるようになったか

本番でだけ確かめられることを、7本の検査で自動的に確かめられるようになった。
切り替えの直後は5本のうち3本が落ちていて、落ちかたが
「クイズが0問」「使える語が0語」だったため、**本番の機能が壊れたように見えていた。**
実際に壊れていたのは検査の数えかたで、機能は動いていた。

新しく確かめられるようになったこと。

- まだ同意していない登録者が、同意して先へ進めること（本番では未確認だった）
- 作者が語を選んで文章を組み、語順と文の切れ目が保存されること
- 端末への通知を頼む入口が出ること。断っても画面が壊れないこと

### 何を引き換えにしたか

答え方そのものを、知らせとサブ指令の検査では見なくなった。
新しい回答画面は「1セクションずつ・長押しで確定」で、その操作を機械に真似させると
押し方が変わるたびに関係のない検査が落ちる。押し方は、それを本題にしている
手元のブラウザ試験と、切り替えの検査が見る。

もう一つ。検査用の利用者を消す道具が、消しながら並べていたせいで取りこぼしていた。
先に全部並べてから消す形に直したので、対象が多いときは時間がかかるようになった。

### どこまでで、どこからが未着手か

済み: 本番向け検査7本（うち新規2本）／同意の往復／語を組む画面／
回答者側の非漏洩（形状アシスト・サブ指令）／知らせの深い検査／通知の入口／
検査用の作品0件／検査用の利用者198人を削除

未着手: 実機で通知の許可を出して宛先が保存されること（手で確かめる）。
消せない検査用の利用者119人（認証サービスが500を返す。SQLでは消える）。

### 触ったファイル

```
scripts/_smoke-http.mjs       同意の確認・語の読み取り・文章の保存を足す
scripts/_smoke-users.mjs      取りこぼしと握りつぶしを直す。下見を足す
scripts/smoke-cutover.mjs     4節を新しい語選びの画面へ。偽の合格を直す
scripts/smoke-shape-assist.mjs  出題の数え方を直す。関門の印を見る
scripts/smoke-sub-directive.mjs 同上。引く操作の時間を測る
scripts/smoke-notice.mjs      答え方を通信へ
scripts/smoke-consent.mjs     新規。同意の関門を1周する
scripts/smoke-push.mjs        新規。通知の入口を確かめる
scripts/smoke-fixtures.mjs    新規。検査用の利用者を本番の入口から片づける
```

アプリのコードと migration は1行も変えていない。

### 次の一手

実機で通知の許可を1台だけ出して、宛先が保存されることを確かめる。

---
## 2026-09-10 本番へ切り替えた（DB 7本 → アプリ 12 commit）

### 何ができるようになったか

本番のデータベースとアプリが、P0〜P5 と origin/main の後発機能の両方を持つ状態になった。
利用者から見て変わるのは次の3つ。カードは1回引けばその枠が決まる（めくって決める2段は消えた）。
規約の同意が、作品を出すときではなく、次に開いたときに求められる。
自分の作品に回答が届くと知らせが出て、許可すれば端末の通知でも届く。

### 何を引き換えにしたか

登録している人のうち、いまの版（2026-08-08）に同意していない人は、次に開いたときに
同意の画面へ送られる。同意するまで先へ進めない。作品を出したことのある34人は
すでに同意済みなので当たらない。本物の作品2件の作者も当たらない。

もう一つ。本番向けのスモーク3本（cutover / shape-assist / sub-directive）は、
回答者の画面を開く手前で同意の関門に当たって止まる。スクリプトが関門を知らないため。
作者側の確認はすべて通っている。

### どこまでで、どこからが未着手か

済み: DB適用（7本を1トランザクション）／履歴の記録7件／未適用0本／構造検査208項目／
関数185・表63／push（早送り 83d1d0f → dd71970）／Production deployment 成功／
公開の検査18項目／本番の引く・確定・引き直し66項目／形状アシスト／サブ指令（作者側）

未着手: 同意の画面を実際に承諾できるかの本番確認（関門に当たる利用者を作れないため）。
本番向けスモークを関門に対応させること。課金2本の版番号の衝突（別件）。

### 触ったファイル

なし（コードは1行も変えていない）。本番へ出したのは既存の12 commit。
Production deployment: dd719709a07fc580e131aa323e7bbe187fbaf92d

### 次の一手

本番向けスモーク3本へ、同意の関門を通る手順を足す。

---
## 2026-09-10 本番へ切り替える道具を作った（7本を1つのトランザクションで）

### 何ができるようになったか

本番へ当てていない7本の migration を、**全部入るか1本も入らないかのどちらか**にできる。
これまでは1本ずつ当てていたので、1本目が入って2本目がまだ、という状態が本番に見えた。
その間、形の取っかかり（D191）と制作の手がかり（D193）が返らなくなる。
1つのトランザクションにまとめれば、切り替わりは最後の一瞬だけになる。

### 何を引き換えにしたか

引き換えは無い。既存の `db:apply:one` は1本も変えていない。1本だけ当てたいときは
これまでどおり使える。新しい道具は2本以上でないと受け付けない。

### どう使うか

```
npm run db:apply:many -- A.sql B.sql ... --dry-run
npm run db:apply:many -- A.sql B.sql ... --confirm-project <link しているプロジェクト>
```

渡した順にそのまま流す。**版番号の順に並べ替えない。**今回の7本は
`20260910190000` を2番目に置く必要があるので、これは仕様である。

当てる先を取り違えないよう、`--confirm-project` で明示させる。link されている先と
一致しなければ、接続する前に断る。プロジェクトの識別子はコードに焼き付けていない。

履歴表には触れない。当てたあと `migration repair` を7回、手で実行する。
SQL が入ったことと履歴に書いたことを、別々の失敗の境目として扱うため。

### 確かめたこと

- 道具の自己試験 21件（名前の取り違え・重複・当てる先の食い違いを断るか、
  途中で落ちたら1本も残らないか）
- 本番と同じ土台（関数144・表52）へ7本を一括で当てて 関数185・表63。8つの鍵が全部返る
- 失敗する SQL を最初・途中・最後の3か所に差し込み、3回とも 関数144・表52 へ完全に復帰。
  P0〜P5 のものが1つも残らないことも確認
- 7本のどこにも、トランザクションに入れられない文が無いこと（18種類を機械で数え、
  行頭の文の種類も目で確かめた）
- 型検査 ／ lint（エラー0） ／ 単体79 ／ 道具21 ／ DB構造197 ／ 縦断343 ／
  アップグレード31 ／ スモーク11 ／ build 成功

### 次の一手

- 本番へ7本を当てる。**まだ当てていない。**当てる前に `--dry-run` で順を目で確かめる
- 当てたあと、7つの版番号を `migration repair` で記録する
- そのまま続けてアプリを出す。データベースだけ新しくアプリが古い時間は短くする

### 触ったファイル

- `scripts/db-apply-many.mjs`（新規）
- `test/tools/run.mjs`（新規。道具の自己試験）
- `package.json`（`db:apply:many` と `test:tools`）
- `scripts/run-all-checks.mjs`（全検査の並びへ追加）
- `docs/launch-checklist.md`（切り替えの4段と、そのあいだに起きること）
- `README.md`（検査の一覧）


---

## 2026-09-10 本番へ出す統合版を作った（origin/main ＋ P0〜P5。11本目）

### 直前に終えたこと

本番のデータベースとアプリに最も近い系統は、手元の main ではなく origin/main のほうだと
分かった。origin/main には管理・プロフィール・形状アシスト・回答の知らせ・サブ指令が入り、
本番のデータベースにもその migration が全部入っている。一方 P0〜P5 は 150edda を起点に
していたので、そのまま本番へ出すと origin/main 側の機能を巻き戻す危険があった。

そこで origin/main（83d1d0f）から新しい作業線 `p0-p5-production-integration` を作り、
P0〜P5 の 10 commit を古い順に持ち込んだ。7本で衝突が出た。どちらか片方を消すのではなく、
両方の機能が最後まで残る形へ寄せた。

### 何ができるようになったか

1本の作業線に、両方の機能が同時に載った。お題を1回で引いて枠ごとに引き直せる作りと、
形の取っかかり・制作の手がかり・回答の知らせ・管理の画面が、同じ画面の中で共存する。

### 何を引き換えにしたか

引き換えは無い。ただし、統合のために2か所を書き直した。
サブ指令を書く場所を、消えた「これに決める」から「1枚引くと決まる」のほうへ移した。
試験の中で盤面を進める補助を、origin/main 側の「めくって決める」から
P0 側の「1枚引くと決まる」へ差し替えた。

### 確かめたこと（すべて統合後の作業線で実測）

型検査 ／ lint（エラー0・警告2） ／ 単体79 ／ 柵30 ／ 配色 ／ 語彙652 ／
DB構造197 ／ 縦断343 ／ アップグレード31 ／ スモーク11 ／ ブラウザ130 ／ build 成功。
本番と同じ土台（関数144・表52）を作り、未適用7本を投入予定順に当てて 関数185・表63。
形状アシスト・サブ指令・完成可否・一巡・引き直し・捨て札の6つが、
開始直後／全枠確定後／引き直し後の3場面すべてで返る。

### 次の一手

- 本番へ7本を当てる。**まだ当てていない。**
- P0 と合流の2本のあいだに、形の取っかかりとサブ指令が返らない短い時間ができる。
  これを消すには2本を1つのトランザクションで流す必要があり、いまの
  `db:apply:one` は1本ずつなのでできない。新しい道具の設計だけ出して止めてある。
- Production deployment の commit は不明のまま（Vercel のトークンが無効）

### 触ったファイル

- `src/app/play/actions.ts`（サブ指令を書く場所を移した）
- `test/e2e/browser.mjs`（両方の試験群を並べ、共通の補助を P5-B 版へ）
- `test/db/run.mjs`（両方の試験群を並べた）
- `scripts/db-checks.mjs`（表の数を実測の63へ）
- `README.md` `docs/test-layers.md` `docs/launch-checklist.md`（件数を実測へ）


---

## 2026-09-10 サブ指令を本番へ出した（D193。DB → push → 本番往復まで完了）

### 何ができるようになったか

お題に出た正式な語1つに、「それをどう描くか」の手がかりが1つ添うようになった。
本番で実際に付いたのを確認している。

```
恐怖   └ 身体がすくんでいる
鳥籠   └ 扉が開いている
水色   └ 一箇所だけ濃く残す
粘着   └ 引き剥がした糸が伸びる
```

対応するのは24語（モーフ8・感情6・性質4・カラー6）に3候補ずつ、合計72候補。
付くのは70%で、30%は何も付かない。候補の無い語には付かない。

### 何を引き換えにしたか

カードを決める操作が、DBへの往復2回になった。本番で31回測って中央値2318ms、
最大3562ms。待たされる感じは出ていない。
後半だけ失敗すると、正式なお題は成立してサブ指令だけ付かない。

表は52のまま。列2つと関数1本を足しただけ。

### 塞いだ穴

開発の途中で、利用者が鍵を捏造して保存できる形になっていた。
カードを決める窓口の引数として鍵を渡していたためで、あの窓口は
サインイン済みなら誰でも直接叩ける（証明書はブラウザから読み取れる）。

直し方は「呼べる相手そのものを変える」。書く窓口を別に作り、
service_role にだけ配った。本番で本物の証明書を取り出して3経路とも試し、
403 / 403 / 404 で断られ、1件も書き込めないことを確認した。

### 本番の実測

- migration 51本（1本だけ単独適用）／ tables 52
- 適用直後のDB実測 18項目すべて合格
- DB verify（本番）208項目すべて合格
- verify:launch 18項目すべて合格
- 本番smoke A〜J すべて合格
- 検査データは全部片づけ、works/prompts/answers/draft_sessions すべて適用前と同数

片づけで1つ取りこぼしがあった。作品を消しても回答の集計行が残り、
DB verify の A20・A22 が落ちた。検査用利用者の集計行だけを消して直した。

### 次の一手

- 対応表を24語から広げるかどうか（いまは網羅していない）
- 未着手のまま: 検索実装 / 未踏組み合わせ / D169再設計 / チュートリアル本体 /
  推薦 / analytics本体 / RPC既定EXECUTE全監査 / D172番号重複
## 2026-09-10 盤面の読み出し口を、先に本番へ入った作業線と合流させた（10本目）

### 直前に終えたこと

同じ名前の関数 `draft_state_json` を、2つの作業線がそれぞれ作り直していた。
P0（この作業木、本番未適用）は引き直し・捨て札・一巡の状態を返すように変え、
D191 / D193（もう片方、**本番へ適用済み**）は形状アシストとサブ指令を返すように変えた。
後から当てたほうが勝つので、P0 をそのまま当てると先に入っているほうの鍵が消える。

`20260910190000_merge_draft_state_contract.sql` を1本足して、両方の鍵を返す形にまとめた。
表も列も作らない。関数1本を作り直すだけ。足す2つの鍵は行をまるごと jsonb にしてから
引く形（`to_jsonb(ds) -> 'shape_assist_key'`）なので、列がまだ無い並びでも落ちない。

当てる位置は P0 の直後。P0 より後の5本は `draft_state_json` を作り直さない（実測）。

### 確かめたこと

- 本番と同じ並び（関数144本・表52個。本番の実測と一致）を手元に作り、
  P0 → 合流 → P2 → P3 → P4 → P5-A → P5-B の7本を当てて成功。最終は関数185本・表63個
- 本番と同じ並びのデータベースを2つ作り、片方だけに7本を当てて JSON の鍵を突き合わせた。
  **消えた鍵は0件**（てっぺん 19→20 / 枠 11→14 / カード 6→7。増えたのは P0 の鍵だけ）
- 形状アシストとサブ指令の値が、引き直しを1回はさんでも残ることを実測
- `is_ready_to_complete` は2つの版で計算の仕方が違うが、実際に始められる2モードでは
  同じ値になった（開始直後 false、全枠確定後 true）
- 型検査・lint（エラー0）・単体79・DB構造179・縦断269・アップグレード31・check:docs 14/14

### 版番号の衝突（別問題。まだ直していない）

`20260909180000` と `20260909200000` を、手元の main の課金2本と、もう片方の作業線が
別々に使っていた。本番に入っているのはもう片方。**課金の2本はこの版番号のままでは
本番へ当てられない。**将来入れるときは未使用の版番号へ振り直す必要がある。
2026-09-09 の記録にあった「課金 v0 は本番へ適用済み」は誤りだった（実測で訂正）。

### 次の一手

- 本番へ7本を当てる（`db:apply:one` で1本ずつ。`db:deploy` は通らない）。**まだ当てていない**
- 当てる前に、本番のアプリの実態が変わっていないかを `/founder` `/tokushoho` `/consent`
  `/api/notifications` の応答で再確認する
- Production deployment の commit は依然として不明（Vercel のトークンが無効）

### 触ったファイル

- `supabase/migrations/20260910190000_merge_draft_state_contract.sql`（新規）
- `docs/decisions.md`（2026-09-10 の節を追記）
- `docs/launch-checklist.md`（未適用7本の一覧と、版番号の衝突の訂正）
- `README.md`（migration の本数）
