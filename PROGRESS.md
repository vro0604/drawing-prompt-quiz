# PROGRESS

セッションの引き継ぎを置く場所です。新しいものが上。
`/next` はここを起点に「現在地と次の一手」を出し、`/progress` がここへ追記します。
日時は JST。全体が400行を超えたら、一番下（最も古いもの）から削ります。

## 2026-09-18 トップを「引く・描く・答える」の押せる3手順に作り直した（D207）

### 何ができるようになったか

初めて来た人がトップを開くと、いちばん上に「あなたの絵、何を描いたか伝わる？」「引いて、描いて、答える。」があり、
その下に「引く」「描く」「答える」の3つの四角が横一列に並ぶ（スマホでも横一列）。四角は説明の図ではなく押せる行き先で、
引く → お題を引く画面、描く → 確定したお題を持っていればそのお題のページ（持っていなければお題を引く画面）、答える → 作品一覧。
最初に押すボタンは「お題を引く」1つだけで、答えるだけの人向けに枠だけの「描かずに、絵に答える」を横に置いた。
古い説明（「お手軽（3枚）と標準（5枚）」。今は「通常」「高難度」）も消えた。

### 何を引き換えにしたか

トップを開くたびに /api/challenge を1回余分に読む（全ページの時計の帯と同じ窓口。未サインインの人には何も作らない）。
読み終わるまでの一瞬、「描く」は /play を指している。「描いた絵で試す」はボタンから文字の入口に下げたので目立たなくなった。

### どこまでで、どこからが未着手か

済み: トップと3つの行き先の接続、320/390/1280px と暗い地での確認、ゲストでの2本の流れ（引く → お題 → 描く側 ／ 答える → 回答）、
ブラウザ試験2件の追加。お題・クイズ・回答・分析・課金・DB は触っていない。
未着手: ヘッダーの行き先がスマホで2〜3段に折り返す問題（トップの範囲外）。Notion の対応表の写真の撮り直し（撮影の道具が本流に無い）。

### 触ったファイル

- `src/app/page.tsx`、`src/app/_home-steps.tsx`（新規）
- `test/e2e/browser.mjs`（A 群に2件）、`README.md`・`docs/test-layers.md`（ブラウザ試験の件数）
- `docs/home-first-use.md`（新規）、`docs/decisions.md`（D207）、`PROGRESS.md`

### 次の一手

- 本番で初見の人に数人見てもらい、「最初に何を押したか」を聞く（数で測る道具は無い）

## 2026-09-18 履歴 20260909180000 を本番で直した（方式 C。変わったのは履歴の1行だけ）

### 何ができるようになったか

本番の migration の記録が、手元の origin/main と1行残らず一致した。20260909180000 の行は、古い課金の下書き（106文）から
実際に入っているプロフィール権限の修正（11文）の記録に置き換わった。これで、本番の履歴からファイルを作り直す操作（`migration fetch`）をしても
同じ版番号のファイルが2つにならず、その後の `db push --dry-run` も止まらない（本番へ書かずに確かめた）。
表・関数・権限・データは1つも変わっていない（部品1,515個・権限378個・public の全表の件数とハッシュが前後で同じ、構造検査は前後とも236/236）。

### 何を引き換えにしたか

本番の履歴表へ1行書き込んだ。戻すときは `node scripts/db-history-0909.mjs rollback --yes` で修復前の行へ戻せる（使い捨ての DB で確認済み、本番では使っていない）。
道具はリポジトリに残っていて、いま流すと「修復後」と判定され、`fix` は何も書かずに止まる。

### どこまでで、どこからが未着手か

済み: origin/main（de46a84）から作った作業木での事前確認（止める条件すべて不成立）、修復、直後の check・構造検査・前後の比較、fetch の経路の確認、記録。
未着手: 無し（この件）。手元の main（6d24ec1）には同じ版番号の古い課金のファイルが残っているので、その作業木から CLI の repair を流さないこと。

### 触ったファイル

- `docs/history-20260909180000.md`（冒頭に本番修復の記録）、`docs/decisions.md`（D206）、`docs/launch-checklist.md`、`docs/history-20260909180000-harness/README.md`
- `scripts/db-history-0909.mjs`（冒頭の説明に「修復済み」を足しただけ）、`PROGRESS.md`

### 次の一手

- この件は完了。次の migration を本番へ当てるときは、いつもどおり `db push --dry-run` から始める

## 2026-09-17 履歴 20260909180000 の食い違いを、使い捨ての PostgreSQL で再現し、3つの直し方を比べた（本番は読み取りだけ）

### 何ができるようになったか

本番の履歴表の食い違いが「1行の名前と文の記録だけ」で、表・関数・権限は手元のファイルどおりだと、機械的な比較で言えるようになった。
本番と同じ履歴・同じ部品・同じ権限を持つ使い捨ての DB を作り、何もしない（A）・CLI の repair（B）・1行だけの SQL（C）を同じ複製元から試した。
B と C は直した後の履歴が1文字も同じで、スキーマ・権限・データ・構造検査236は前後で変わらず、どちらも元の履歴へ1文字違わず戻せた。
後ろに migration を1本足しても3つとも正常。放置したときに壊れるのは、履歴を正本にする操作（`migration fetch` の後の `db push`、履歴だけからの作り直し）だけだった。

推奨は C。前提（作業木のファイル・履歴67行・対象行・ほかの66行・CLI の list と dry-run）を確かめてから1行だけ書き換え、違えば何も書かずに止まる道具
`scripts/db-history-0909.mjs` を作り、使い捨ての DB で直す・戻す・止まるべき5場面を通した。本番には `check`（読み取り）だけを流し、「修復前」と判定された。

### 何を引き換えにしたか

直す道具は「本番の履歴が67行」を前提にした値を持つ。次の migration が先に本番へ入ると、道具は止まるので、`docs/history-20260909180000-harness/` で値を作り直す必要がある。
`--linked` での実測と、Docker が要る `db pull` / `db diff` は測っていない。106文の下書きがどの時点のファイルだったかは git に無く、原因は推定のまま。

### どこまでで、どこからが未着手か

済み: 本番の読み取り（履歴・部品・権限・list・dry-run・構造検査236）、正常対照と本番に似せた DB、A/B/C の前後・後続 migration・戻す・再適用、
repair の正体（届いた文と前後）、fetch と履歴からの作り直し、イベントトリガーの発火、道具の通し稽古、既存の DB 試験（縦断424・アップグレード31・構造225・同時6・柵30）。
未着手: 本番で直すこと（ユーザーの判断待ち。今回は実行しない指示）。

### 触ったファイル

- `docs/history-20260909180000.md`（全面改訂）、`docs/history-20260909180000-harness/`（新規・再現の道具）、`docs/decisions.md`（D206）
- `scripts/db-history-0909.mjs`（新規）、`supabase/manual/20260909180000_history_fix.sql`・`_history_rollback.sql`（新規）
- `PROGRESS.md`

### 次の一手

- ユーザー: 本番で直すかを決める（推奨は C、急ぎではない。行うなら次の migration より前）
- 直す場合の手順・止める条件・戻し方は `docs/history-20260909180000.md` の 10 にある。→ 2026-09-17T15:27Z に実行済み（上の節）

## 2026-09-17 一般公開の前に残る非課金の問題を、本番を実際に操作して洗い直した（課金は対象外）

### 何ができるようになったか

本番を、利用者と同じようにブラウザで押して一周する確認ができるようになった（`npm run smoke:prod -- journey`）。
確認メールのリンクを別のブラウザで開く → 同意 → お題を引いて確定 → 画像つきで投稿 → 共有 → ゲストがスマホ幅で長押しして回答 →
登録者が回答・保存・通報の画面 → 作者に知らせと結果 → 削除 → サインアウト、までを本番で通し、すべて合格した。
`JOURNEY_LONG=1` を付けると、長い英数字の題名・表示名と横長の絵で同じ一周をする。

結論は「まだ一般公開できない」。利用者の流れは壊れていない。止めているのは判断と人の作業で、
一般公開への切り替え（規約の「限定公開です」の一文と検索除外）、本物の作品が2件しか無いこと、
通報の送信と Supabase の管理画面の設定（匿名サインインの上限・確認メールの本文・バックアップ）が未確認なこと。
分類の全体は `docs/launch-audit-2026-09-17.md`。

本番の smoke:work が、お題に「取引」が出るとフッターの「特定商取引法に基づく表示」を答えの漏れと数えて落ちていたので、
数える前にフッターを外すよう直した（検査の道具だけ。アプリのコードは変えていない）。

### 何を引き換えにしたか

一周の確認は本番へ書き込む（使い捨ての登録者2人・ゲスト1人・作品1件・回答2件）。作品は画面から消し、登録者は消すが、
ゲストと削除済みの作品の行、使い捨ての人の同意記録（持ち主は空）は残る。通報の送信は本物の CAPTCHA なので確かめられない。
画面のはみ出しは数字（横幅）では拾えない形があった（外側で切られる）。画面写真を見て判定する必要がある。

### どこまでで、どこからが未着手か

済み: 本番の通信の検査10本と画面の一周3回、DB の検査236項目と整合の読み取り、コードと文書の未完了の突き合わせ、5分類、test:all 16/16。
本物の作品2件と本物の利用者4人の行指紋は前後で同じ。検査用の固定利用者27人は片づけ済み（残り0）。

未着手: 本番のログ（Vercel・Supabase）は見られていない（Vercel CLI 未ログイン、管理 API の鍵に触れる操作は権限で拒否）。
A（公開阻害）は4件ともコードでは直せない（判断か人の作業）。B〜E はどれも未着手。

### 触ったファイル

- `scripts/smoke-journey.mjs`（新規）、`scripts/_smoke-http.mjs`、`scripts/run-production-smoke.mjs`
- `docs/launch-audit-2026-09-17.md`（新規）、`PROGRESS.md`

### 次の一手

- ユーザー: 一般公開に切り替えるかの判断（規約の文言は課金の作業線の規約 v2 と一緒に直す必要がある）、本物の作品を増やす、通報を1回送る、Supabase の管理画面で3つの設定を見る
- Claude（非課金）: Next.js を 16.3.3 以上へ上げる（B1）→ URL に載せた任意の文が告知の枠に出る問題（B2）→ 長い英数字でカードがはみ出す問題（B3）

## 2026-09-17 Stripe 実通信の結果を正式に反映した（D201 の返金の期待・購入の要求が重なったときの表示）

### 何ができるようになったか

- 同じ人が2つのタブから同時に購入を押しても、画面に Stripe の英語のエラーが出なくなった。
  重なったほうには「処理が重なりました。少し待ってから、もう一度お試しください。」と出て、もう一度押せば同じ決済ページへ進む。
  Stripe の元のエラーはサーバーの記録にだけ残る。二重購入を止める仕組みと、売り切れ・購入済み・販売停止の返し方は変えていない
- D201 の6段目の期待を「最終的に返金済み。途中で返金手続き中を通ることがある」に改めた（ユーザー指示）。
  権限の取り消し・枠の戻り・欠番・番号の再利用なしは、変えずに必ず確かめる
- Stripe のテストモードで、改定後の道具を最後まで通した（68項目すべて合格。重なりは受け口と画面の両方で再現して確かめた）

### 何を引き換えにしたか

重なったときの応答が 500 から 409 に変わった（合図 `CHECKOUT_BUSY`）。409 はもともと売り切れ・購入済みでも使っているので、
画面や将来の呼び出し側は、409 を合図（code）で見分ける必要がある。いまの購入ボタンは本文の文言を出すだけなので影響は無い。

### どこまでで、どこからが未着手か

済み: 改定、実装、単体2件、`local` 50・`stripe` 68、全回帰試験、push と本番 deploy、販売停止のままの本番スモーク。
次の工程の入口: `docs/history-20260909180000.md`（履歴 20260909180000 の食い違いを、使い捨ての PostgreSQL で修復手順ごと再現する。本番では repair しない）。

未着手: 想定外の失敗（`billingError` の unknown）は、これまでどおり元の文を画面へ返す。Stripe のほかの失敗の文が出る余地は残っている（今回の範囲外）。

### 触ったファイル

- `src/features/billing/conflict.ts`（新規）、`src/app/api/billing/checkout/route.ts`
- `test/unit/run.mjs`、`test/billing/stripe-e2e.mjs`
- `docs/decisions.md`（D201 の6段目と注、D205 の反映）、`docs/billing-stripe-test.md`、`docs/history-20260909180000.md`（新規）、`PROGRESS.md`

### 次の一手

- 履歴 20260909180000 を、`docs/history-20260909180000.md` の 2 の順に使い捨ての PostgreSQL で再現する（読み取りと検証だけ）

## 2026-09-17 Stripe テストモードへ実際に通信し、D201 の8段を最後まで通した（D205。販売はしていない）

### 何ができるようになったか

課金 v0 が、Stripe のテストモードを相手に「決済ページ → テストカードで支払い → 知らせ → 確定 #001 → 権限 → 公開設定 → 返金 → 欠番 → 枠が戻る」
まで本当に動くことを確かめた（60項目すべて合格）。二重の知らせ・買い終えた人の再購入・同時2回・残り1枠に2人・不正な知らせも同じ回で見た。
販売を開けたのは手元の DB の中だけで、本番の商品は販売停止のまま、本番の購入などは0件のまま。

実通信で**バグを1つ見つけて直した。**Stripe の口座で Managed Payments（Stripe が販売者になる仕組み）が既定で有効だと、
購入の受け口が毎回 500 になり、誰も決済ページへ進めなかった。決済ページの要求ごとに Managed Payments を切るようにした。

### 何を引き換えにしたか

Managed Payments（税の代行・Stripe が販売者）を、この決済ページでは使わないことを明示した。今の仕様（販売者はこのサービス、税込み、カードだけ）どおり。
使うかどうかは別の判断として残る。

### どこまでで、どこからが未着手か

済み: 実通信の8段（4回目で全合格）、修正と単体試験、全回帰試験、main へ push と本番 deploy、本番の確認、Stripe 側の片づけ（顧客0・開いた決済ページ0）。

未着手（提案。採否は未回答）:
- D201 の6段目の期待「refund_pending → refunded」は、テストカードの返金がすぐ成立するため Stripe では起きなかった。期待の読み替え
- 2つのタブから同時に購入を押すと、2回目に Stripe の英語のエラーがそのまま出る（二重購入にはならない）。文言の修正
- 本番用（live）の Stripe の鍵と受け口、販売開始はどれも未着手

### 触ったファイル

- `src/features/billing/signature.ts`（決済ページの要求の組み立てと Managed Payments を切る）、`src/features/billing/stripe.ts`
- `test/unit/run.mjs`（1件追加）、`test/billing/stripe-e2e.mjs`（失敗時の記録・片づけ・判定の誤り3件）
- `docs/decisions.md`（D205）、`docs/billing-stripe-test.md`、`PROGRESS.md`

### 次の一手

- 上の提案2件の採否
- 本番（live）で1段目（決済ページが開く）だけ確かめるかどうかの判断（D201: 本番の返金試験はしない）

## 2026-09-17 課金 v0 を D201 の8段で一周させる道具を作った（D205。Stripe との通信は鍵待ち）

### 何ができるようになったか

課金 v0 を「予約 → 決済ページ → 支払い → 知らせ → 確定 → 番号 → 権限 → 公開設定 → 返金 → 欠番 → 枠が戻る」の順に、
手元のアプリと手元の DB の中だけで一周させられるようになった。本番の DB にも本番の商品にも触れない
（販売を開けるのは手元の PGlite の中だけ）。

- Stripe と通信しない形（既定）: 50項目すべて合格。3〜8段、二重処理、不正な知らせ5種、突き合わせの失敗6種を本物の受け口で確かめた
- Stripe のテストモードへ実際に通信する形（`-- --mode stripe`）: 道具は作ったが、**未実施。**Stripe の鍵がこの端末のどこにも無い
  （環境変数・.env.local・キーチェーン・ホーム以下のファイルを探して0件。Stripe CLI のログイン情報も無い）

### 何を引き換えにしたか

`stripe` モードは Stripe の決済ページ（checkout.stripe.com）を Playwright で操作するので、Stripe が画面を変えると入力欄の指定が壊れうる。
ほかの引き換えは無し（本番のコードは変えていない）。

ブラウザ試験の誤判定を1件直した（D205 の新規発見）。お題に「取引」が引かれると、フッターの「特定商取引法に基づく表示」を答えの漏れと数えて落ちていた。
数える前にサイト共通のフッターを外す。

### どこまでで、どこからが未着手か

済み: 8段の定義を D201 から復元、案 A の組み直し（Webhook の受け口を管理画面に作らず、stripe listen で転送）、
道具（local 50項目合格）、鍵が無い・sk_live_ の形の鍵では通信前に止まることの確認、Stripe CLI v1.50.11 の取得と照合、全回帰試験、記録。

未着手（人の操作が要る）: Stripe テストモードの秘密鍵をキーチェーンへ入れる（手順は docs/billing-stripe-test.md の 2。人がやるのはこれだけ）。
そのあと Claude が `stripe` モードで1〜8段・連打・枠の競合・返金を通す。

### 触ったファイル

- `test/billing/stripe-e2e.mjs`（新規）、`package.json`（test:billing:stripe）、`test/e2e/browser.mjs`（誤判定の修正）
- `docs/billing-stripe-test.md`、`docs/decisions.md`（D205・D201 の追記）、`README.md`、`PROGRESS.md`

### 次の一手

- ユーザー: docs/billing-stripe-test.md の 2 の 1〜8
- Claude: `STRIPE_CLI=<CLI のパス> npm run test:billing:stripe -- --mode stripe`

## 2026-09-17 課金 v0 の本番着地を、別の作業線とは独立に確かめ直した（D204。販売はしていない）

### 何ができるようになったか

課金 v0 が本番の DB に正しく入っていることを、先に当てた作業線の記録に頼らず測り直した。
本番の履歴67本のうち66本は、履歴に残った SQL の文がすべて手元のファイルに含まれる。
課金の関数20本（持ち主・definer・search_path・権限・定義の md5）、表5つ、索引16、起動3、制約42、ポリシー0が、
使い捨ての PostgreSQL 17.6 に本番と同じ順で65本 → 課金1本目 → 2本目を当てた結果と1文字まで同じ。
構造検査は236項目すべて期待どおり（前回の不合格17は0）。商品は `is_active = false` のまま。
購入・権限・顧客・受け取った合図はすべて0件。仕組みの回答0件・待ち行列0行。

課金の入口が販売停止のまま閉じていることを本番で見るスモーク `smoke:prod -- billing` を足した
（画面3つ＋購入の受け口＋知らせの受け口、未サインインと登録利用者の両方。本番で全項目合格）。

### 何を引き換えにしたか

`smoke:prod -- billing` は「販売停止中」を期待値にしているので、販売を始めた日に落ちる。
そのときに期待値を直す（落ちることで販売開始に気づける作り）。ほかの引き換えは無し。

### どこまでで、どこからが未着手か

済み: 開始状態の取り直し、課金の仕様をコードから再構成、履歴と SQL の突き合わせ、使い捨て DB での2段の再現（47項目）、
手元サーバーでの署名の確かめ（偽の署名・古い署名・書き換えた本文を 400、8項目）、test:all 16工程、本番スモーク、
検査用利用者15人の片づけ、記録。本番の DB へは今回何も当てていない（07:22Z に別の作業線が当てたものを確かめた）。

記録の訂正: 規約・ポリシーの有効版は 2026-09-08 ではなく 2026-08-08 → 2026-09-09 に移った（本番の terms_versions は 08-04・08-08・09-09 の3行）。

未着手:
- Stripe テストモードの鍵と Webhook の登録（人の操作。本番の Vercel に STRIPE_SECRET_KEY / STRIPE_WEBHOOK_SECRET は無いと受け口の 503 から推定。Vercel の API は forbidden で直接は読めていない）
- 販売開始（`is_active = true`）。コードからは開けず、SQL を1本流す運営操作。今回は行っていない
- 履歴 20260909180000 の名前の食い違い（中身は古い課金の文 106本。課金の適用には影響なし）

### 触ったファイル

- `scripts/smoke-billing.mjs`（新規）
- `docs/decisions.md`（D204）、`docs/launch-checklist.md`、`docs/billing-stripe-test.md`、`PROGRESS.md`

### 次の一手

- Stripe テストモードの鍵を用意し、`docs/billing-stripe-test.md` の 3（推奨は案 A: 手元の DB と Stripe テストモード）で8段を通す

## 2026-09-17 課金 v0 を本番の DB へ当てた（販売はしていない／Stripe の試験は鍵待ち）

### 何ができるようになったか

本番のデータベースに課金 v0 が入った。表5・関数20・索引4、商品 `founding_creator_v0`（3,000円・30枠）が1行。
**商品は `is_active = false` のままで、販売していない。**購入・権限・顧客・受け取った合図はすべて0件。
規約とポリシーの有効版が 2026-08-08 → 2026-09-09 に移った（ユーザー承認。当初 2026-09-08 と書いたのは誤り、D204 で訂正）。メールで登録した本物の4人は、
次に守られた画面を開くと再同意を求められる（4人とも needs_consent = true を実測。同意記録には触れていない）。
再同意の画面は、使い捨ての登録者で本番を1周して確かめた（表示 → 同意 → 記録 → 関門が外れる。版は 2026-09-09）。

`/founder` と `/tokushoho` は、販売停止中であることを明示するようにした。
直す前は、未ログインで `/founder` を開くと価格・「残り30枠」・「ログイン・登録の画面へ」が出て、
販売していないことが書かれていなかった（DB を当てた直後の本番で実測）。

### 何を引き換えにしたか

本物の4人は、次に投稿などの守られた画面を開くとき、同意の画面を1回通る。
販売停止中の `/founder` は、ログインや登録へ誘わなくなった（買えない先へ誘わないため）。

### どこまでで、どこからが未着手か

済み: 本番での下見（1トランザクションで2本を流して全部確かめて rollback。期待と違う0件）、
`db:deploy` での適用（07:22:19Z〜07:22:28Z）、履歴67/67、db:verify:keychain 236項目すべて期待どおり、
再同意の本番1周、使い捨ての登録者の片づけ（1人）。
**未着手（人の操作が要る）: Stripe テストモードの鍵と Webhook の登録。**鍵は手元にも環境変数にも無く、
Vercel CLI は未ログイン。手順は `docs/billing-stripe-test.md`。
**未決定: D201 の8段の1段目は、販売停止中の商品では予約の関数が OFFER_CLOSED で止める。**
本番でそのまま通すには商品を一時的に販売中にするしかない（指示で「停止して報告」）。案 A〜C と推奨は同文書の 3。

### 何が何を呼ぶか

画面（/founder・/tokushoho）→ `get_founder_offer_status` が返す `is_open`（DB の `billing_offer_is_open`、
`is_active` と販売期間で決まる）を見て、false なら「販売しておりません」を出す。
購入の受け口は、設定の確認 → ログイン → `billing_reserve_slot`（販売停止なら OFFER_CLOSED）の順で、
Stripe を呼ぶのはその後。知らせの受け口は、鍵の確認 → 署名の確認のあとでだけ DB に触る。

### 触ったファイル

- `src/app/founder/page.tsx`・`src/app/tokushoho/page.tsx`（販売停止の明示）
- `docs/billing-stripe-test.md`（新規）、`docs/decisions.md`（D201 の追記）、`docs/launch-checklist.md`、`README.md`
- `scripts/db-checks.mjs`（コメントだけ）

### 次の一手

- Stripe テストモードの鍵を用意し、`docs/billing-stripe-test.md` の 3 で試験の環境（案 A〜C）を決める

## 2026-09-17 運営の利用状況を人と仕組みに分けた／smoke-unseen を読み込み完了で読む形へ／etag の変化の原因（D202 / D203）

### 何ができるようになったか

運営用の利用状況（get_usage_summary）で、回答完了を「人が答えた数」（answer_completed）と
「仕組みが答えた数」（answer_completed_system）に分けて見られる。本番へ適用済み。
いまは仕組みの回答が0件なので、本番の値は前と同じで、仕組みの側は0。

本番スモーク smoke-unseen が、実際のブラウザで開いて知らせの入口の読み込み完了を待ってから読むようになった。
前は HTML の仮の値（いつも「無し」）を読んでいて、何も確かめていなかった。

公開サイトの etag が push の前に変わった件は、同意の保存がサイト全体のキャッシュを作り直すためと分かった（D203）。

### 何を引き換えにしたか

get_usage_summary の定義が1本入れ替わった。権限・持ち主・戻り値の型は同じ。返す jsonb の鍵が1つ増えた
（呼び出し元はアプリに無い）。smoke-unseen は Playwright を使うようになり、実行が数秒遅い。

### 何が何を呼ぶか

運営が service_role で `get_usage_summary(日数)` を呼ぶ → answers を answer_source で human / system に分けて数え、
ほかの4つ（共有・次の作品・制作開始・持ち出し）と一緒に jsonb で返す。
smoke-unseen: `smoke:prod -- unseen` → Chromium で / /works /rankings を開く → ブラウザが /api/notices/unseen を呼ぶ →
入口が data-loaded="true" になるのを待つ → 入口の状態と、ブラウザが受け取った答えを突き合わせる。

### どこまでで、どこからが未着手か

済み: migration 20260916120000 の作成と本番適用、試験（SU 群5件・構造 A63）、smoke-unseen の書き直し、
etag の原因の特定、全試験、push と deploy、本番確認。

未着手:
- 課金 v0 の2本（20260917090000 / 100000）の本番適用（別の作業線。本番の構造検査の17項目はこれが未適用のため）
  → 2026-09-17T07:22Z に別の作業線が適用済み。構造検査は236項目すべて期待どおり（D204 で確かめ直した）
- 履歴 20260909180000 の名前の食い違い
- 仕組みの回答を実際に作る仕組み（外部 AI・自動の積み込み・画面）

### 次の一手

課金 v0 の本番適用は課金の作業線で判断する。システム回答の起動前の技術的な後始末は、この節で残り0件。

### 触ったファイル

- `supabase/migrations/20260916120000_usage_summary_human_answers.sql`（新規）
- `test/db/run.mjs`（SU 群）`scripts/db-checks.mjs`（A63）`scripts/smoke-unseen.mjs`
- `docs/decisions.md`（D202 / D203）`docs/spec.md` `docs/launch-checklist.md` `docs/landing-d194-d196.md` `README.md` `docs/test-layers.md` `PROGRESS.md`

---
