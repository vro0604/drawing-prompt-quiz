# PROGRESS

セッションの引き継ぎを置く場所です。新しいものが上。
`/next` はここを起点に「現在地と次の一手」を出し、`/progress` がここへ追記します。
日時は JST。全体が400行を超えたら、一番下（最も古いもの）から削ります。

## 2026-09-18 作品一覧を「絵そのものがカード」に作り替えた（D211）

### 何ができるようになったか

作品一覧が、Pinterest と同じ形で絵を並べるようになった。
これまでは決まった高さ（192px）の枠に絵を縮めて入れ、その下にタイトル・作者・部門・完成度を並べていた。
いまは絵の縦横比そのままで、高さは1枚ごとに違い、段違いの列に積み上がる。通常の状態で文字は1つも出ない。
広い画面ほど列が増える（幅390で2列・768で3列・1280で5列・1920で8列）。

タイトルを一覧に出すのをやめた。タイトルはクイズの答えの手がかりになりうるため、画像の代替文にも入れていない。
読み上げには、代わりにリンクの説明（「〈作者〉さんの作品。クイズに答える」）が読まれる。

パソコンで絵に触れたときだけ、小さな情報のタブが絵の上に浮く。中身は作者のアイコンと名前・回答済みの印・AIの札・完成度の札・保存・「…」。
「…」の中は 共有 / 興味なし / 通報 の3つで、どれも実際に動く（共有は端末の共有画面かリンクの複写、興味なしはその人の一覧からだけ消す新しい仕掛け、通報は既存の画面へ移る）。
指で触る端末では、この情報のタブは出ない。キーボードの移動先にもならない。出るのは絵だけ。

絵を押したときの行き先が2つに分かれる。まだ答えていない作品はそのままクイズが開き、もう答えた作品は結果と鑑賞が開く。
あいだに「まず作品の説明を読む画面」を挟まない。

いいねは、カードを 0.5 秒長押しすると付く。押している間、中央からパステルピンクが外へ広がり、満ちた瞬間に成立して四隅へ届く。
付いた作品には細いピンクの枠とごく小さな粒が出る。もう一度長押しすると外れる。スマホでは成立の瞬間に軽く震える（対応していない端末でも機能は成立する）。
長押しはスクロールを1ミリも止めない。

ページ送りをやめ、下まで来たら続きを読む形にした。クイズへ入って答えずに戻ると、読んだぶんもスクロールの位置も元に戻る。

公開作品が1件も無いときは、真っ白にせず見本のカードを10枚並べる。見本は押せない・触れても何も出ない・カーソルも変わらない。作品が1件でもあれば見本は1枚も出ない。

### 何を引き換えにしたか

- 一覧から文字が消えたので、**題名で作品を探せなくなった。**本番スモーク4本と公開前検査1本が題名で照合していたので、作品のIDで見る形に直した。
- 画面の外に出たカードは、場所は取り続けるが中身の組み立てをブラウザに省かせている（content-visibility）。古いブラウザでは省かれず、枚数が増えたときの重さが戻る。
- 「興味なし」で表が1つ増えた（70表）。読み書きは新しい関数だけを通り、件数も一覧も外へ返さない。
- 一覧を取る入口が3つ並ぶ状態になった。既存の `get_public_works`（旧5引数・新6引数）はどちらも触っていない。新しい画面だけが `get_feed_works` を呼ぶ。
- 手元で検証用のサーバーへ向けたとき、画像の最適化を許す設定（`dangerouslyAllowLocalIP`）を1つ足した。配信元が 127.0.0.1 のときだけ true になり、本番では必ず false。

### どこまでで、どこからが未着手か

- 触ったのは作品一覧と、そこから押したときの行き先の順番だけ。結果画面・プロフィール・コメント・推薦・投稿画面は1行も変えていない。
- 回答が終わったあとに関連作品へ変形する仕様（将来の別作業）は、いまの構造を壊さずに載せられる形にしてある。
- Pinterest の「触れたときのUI」は実測できていない（サインインしないと DOM に出ない）。そこだけ Pinterest に似せておらず、指示6に書かれた形で作った。
- iOS の Safari 実機では確かめていない。手元にあるのは Chromium だけで、長押しとスクロールの共存は Chromium の指の操作（CDP のタッチ）でしか見ていない。

### 何が何を呼び、値がどこからどこへ渡るか

サーバー側の `/works` が `get_feed_works` を1回だけ呼び、返った行に画像のURLを2つ足して（`features/feed/present.ts`）、ブラウザ側の部品 `_masonry.tsx` へ渡す。
その部品は、画面の幅から列の数を決め（`features/feed/layout.ts`。式は Pinterest の実測そのまま）、DB が持っている画像の縦横比だけで全部の高さを先に決めてから並べる。だから画像が1枚も届く前に場所取りが終わっている。
下まで来ると `loadFeedPageAction` がサーバーへ続きを頼み、返った行を末尾に足す。前に置いたカードの行き先は変わらない。
長押しは `features/feed/press.ts`（時間と座標の勘定だけ。画面が出てこない）が判定し、成立したら `toggleFeedLikeAction` が `toggle_like` を呼ぶ。広がりの見た目だけは React を通さず、押されているカードの中の1要素へ直に幅と高さを書く。

### 本番へ出したあとの確認（2026-09-18）

DBを先に当て、そのあと画面を差し替えた。適用した migration は1本（`20260918120000_feed_masonry`）で、
当てた瞬間に走るのは 表1・索引1・関数3・権限9・RLS1。破壊や更新にあたる文は1本も無い。
当てた直後に、まだ差し替えていない旧い画面で `/` `/works` `/works?tab=ai` `/play` `/rankings` がすべて 200 で開くことを確かめた。

画面を差し替えたあと、本番を実際のブラウザで確かめた。読むだけの確認が24件、押して書き込む確認が13件、どちらも全部合格。

- 幅1280で5列・1列235.19px・隙間16px・端20px。**Pinterest の実測値と同じ**
- 幅390で2列・1列182.5px・隙間9px・端8px。横のはみ出し0
- 通常のカードに文字が1つも出ていない。触れる前は情報子タブの濃さが0
- 触れると 保存・「…」・作者・完成度の札 が出る。「…」の中は 共有・興味なし・通報
- 未回答のカードの行き先が `/works/<id>?q=1` で、開くとクイズが作品の情報より先に出る（実測: クイズ193px / 情報1623px）
- 保存を押すと本番に行が1つ増え、もう一度押すと消える（0→1→0）
- 0.5秒の長押しでいいねが付き、もう一度でほどける（0→1→0）。作品側の控えの数も元へ戻る
- 「興味なし」を押すとその人の一覧から消え、同じ時刻に別の窓（未サインイン）には残っている

**作った行は同じ実行の中で全部戻した。**使った検査用の利用者も `smoke:fixtures:purge` で消した。
確認のあとの本番は 人40／登録済み4／検査用0／いいね0／保存0／興味なし0／公開作品2 で、
`docs/smoke-selfclean.md` 10節の基準値と一致する。`db:verify:keychain` は 239項目すべて合格。

確かめられていないこと。**本番の公開作品が2件しかないので、読み足し（無限スクロール）と
戻ったときの位置の復帰は本番で動かせていない。**どちらも手元のブラウザ試験では通っている。
iOS の Safari 実機も確かめていない。

### 画面と機能の対応表（Notion）

CLAUDE.md の決まりに従って更新した。ページはここ:
https://app.notion.com/p/3d558e61e66e81ec8d20c4a2f2e93cc0

- `/works ｜ 作品一覧` の節を書き直した。説明（絵だけが出ること・列の数・読み足しと復帰）と、
  押せるものの表（情報子タブの4行・長押しのいいね・行き先が2つに分かれること・0件のときの見本）
- 逆引きの表に「共有」「興味なし」「作品を押してクイズを始める」「答えた作品を見返す」を足し、
  「いいね」と「お気に入り保存」を一覧からの操作に書き直した
- `/works/[id]` の節に、一覧から来たとき（?q=1）はクイズが先に出ることを書いた
- 冒頭の最終更新と、更新履歴に1行
- 一覧の新しい写真（パソコン幅・手のひらの端末幅）を2枚入れた

残っている手作業が1つある。**作り替える前の古い写真2枚が、同じ節に残っている。**
私の持っている道具に Notion のブロックを消す手段が無い。どれが古いかは、
その場に置いた灰色の囲みに書いてある（「すぐ下に続く2枚は古い写真です。消してかまいません」）。

写真を撮る道具（`scripts/capture-screens.mjs` / `summarize-screens.mjs`）は、いまも本流に入っていない。
別の作業木に未コミットで置かれているものを、この回だけ一時的に持ち込んで走らせ、
出力（`docs/screens/2026-09-18/` の36画面と `controls.md`）だけを残した。道具そのものは持ち込んでいない。

### 触ったファイル

新規: `src/features/feed/`（types / layout / press / rpc / present）、`src/app/works/(list)/_masonry.tsx`、`src/app/works/(list)/actions.ts`、`supabase/migrations/20260918120000_feed_masonry.sql`、`public/sample-art/`（10枚）、`scripts/make-sample-art.mjs`、`test/e2e/png.mjs`、`docs/RESEARCH/2026-09-18_pinterest-masonry.md`。
変更: `src/app/works/(list)/page.tsx`、`src/app/works/[id]/page.tsx`（`?q=1` のときクイズを先に出す）、`src/features/work/types.ts`、`src/app/globals.css`、`next.config.ts`、`scripts/db-checks.mjs`、`test/`（unit / db / e2e）、本番スモーク4本と `scripts/verify-launch.mjs`、`docs/decisions.md`（D211）。

---
## 2026-09-18 本番スモークが、自分の作った匿名ゲストを自分で消すようにした（D210）

### 何ができるようになったか

本番で画面を1周する検査（`npm run smoke:prod -- journey`）を何回走らせても、終わったあとの本番に痕跡が残らなくなった。
以前は1回走るごとに匿名のゲストが1人ずつ本番に残り、利用者数にも初回利用ファネルにも混ざっていた（実測: 2026-09-17 の2回で40人→42人）。
残った理由は、ゲストを作っているのがスモークではなくサーバー側で、スモークが自分の作った相手を知らなかったこと。
既存の片づけは電子メールの形で検査用を見分けるので、電子メールを持たない匿名ゲストには当たらなかった。
いまはゲストの ID を、作られたその場で2つの経路（ブラウザのセッション Cookie の復号／この実行が投稿した作品への回答行）から拾い、最後にその ID だけを消す。
本番で3回実証した（最後まで成功・ゲスト作成直後に強制停止・次の作品の直後に強制停止）。3回とも8つの表の件数が開始前と完全に一致した。
すでに残っていた2人も、18項目の指紋を照合して全部一致したうえで削除した。本番の人は 42人 → 40人。

### 何を引き換えにしたか

スモークが不合格になる条件が1つ増えた。片づけに失敗すると、一周そのものが成功でもスモーク全体が不合格になり終了コード1で終わる。
これは意図した引き換えで、「片づけの失敗は握りつぶす」（D85）を人を作る経路にだけ適用しないことにした。
`kill -9` や端末ごとの強制終了で `finally` が走らない場合は、これまでどおり残る（30日後に定期の掃除が拾う）。
アプリのコードは1行も変えていないので、画面の見え方は変わらない。

### 何が何を呼ぶか

`npm run smoke:prod -- journey` → `scripts/smoke-journey.mjs`。
一周を始める前に `scripts/_smoke-baseline.mjs` が8つの表を数える。
ゲストが生まれる地点（回答の送信）の直後に `scripts/_smoke-actors.mjs` の `actorFromCookies()` がブラウザの Cookie を復号し、同時に自分の作品への回答行も読んで、ID を帳面に積む。
`finally` で、画像 → 利用の記録・同意の記録 → 作品・お題・ドラフト（`scripts/_smoke-own-rows.mjs`）→ 人（`_smoke-actors.mjs`）の順に消し、最後にもう一度8つを数えて開始前と突き合わせる。
1件でも違えば不合格。`JOURNEY_FAIL_AFTER=guest|next-work` を付けると途中でわざと止められる（片づけの故障試験用）。

### 次の一手

限定公開を広げて実利用者が入ったら、同じ `npm run db:funnel` で測り直し、検査の痕跡だけだった時期の数字と比べる。
閲覧・押下の記録を足すかどうかはそのあとで決める（今回も1つも足していない）。

### 同じ穴が journey 以外にもあった

`scripts/smoke-anon.mjs` も本番でゲストを残す作り（同ファイル44行目に明記）。
1本ずつ直すのをやめ、すべてのスモークが使う共通の入口（`scripts/_smoke-http.mjs` の `finish()`）で、開いた入れ物の Cookie から人を拾って消すようにした。
本番で `smoke:prod -- anon` を1回流し、匿名ゲスト3人が消えることを実測した。
固定の検査用利用者（`dpq-fixture-`）だけは使い回す前提なので消さない。本番で流したあとは `npm run smoke:fixtures:purge` を別に流す必要がある。

### 本番の出発点を作り直した

承認を得て、確認で残った固定の検査用利用者2人を本番向けの入口から削除した（消す前に8項目で独立に照合。対象ちょうど2人・残す40人は1人も含まない）。
本番の人は 42人 → 40人、登録済みは 6人 → 4人。検査用の利用者は0人になった。
既存の purge は利用者を消すだけなので、この2人のドラフト2件は道連れで消え、お題2件・作品2件（どちらも削除済み・非公開）・同意4件は持ち主が外れて残った。持ち主の外れた行はファネルのどの段にも入らない。
この時点の数字を「スモーク自己片づけ導入後の基準値」として docs/smoke-selfclean.md 10節に置いた。以後の増減はここから測る。

### 触ったもの

`scripts/_smoke-actors.mjs`（新規）、`scripts/_smoke-baseline.mjs`（新規）、`scripts/smoke-journey.mjs`、`scripts/_smoke-http.mjs`、`scripts/_smoke-users.mjs`、`test/tools/run.mjs`（片づけの試験20件を追加。46→67）、`docs/smoke-selfclean.md`（新規）、`docs/funnel-v0.md`、`docs/decisions.md`（D210）、`README.md`。

---

## 2026-09-18 初回利用の進みぐあいを、いまの表だけから数える道具を作った（D209）

### 何ができるようになったか

`npm run db:funnel` と打つと、本番を読むだけで「来た人がどこまで進んだか」が人数と率で出る。
過去24時間・7日・30日・全期間の4つ。中身は4つの流れで、作る側（お題が先／絵が先）・答える側・登録。
新しい記録の仕組み（解析サービス・追跡の cookie・閲覧の記録）は1つも足していない。DB の migration も無い。
分母は「DB に現れた人」＝初めて何かを書き込んだ人で、トップを見た人ではない。
このサービスは書き込みの直前まで利用者の行を作らないので、「トップ閲覧者の何％」は作れない。道具自身がそれを毎回出力する。

### 何を引き換えにしたか

引き換えは無い（読むだけで、本番の行は1つも変わらない。前後で7つの表の件数が同じことを確認した）。
ただし数字の使い道には限界がある。匿名のゲストは電子メールを持たないので、検査（スモーク）が作ったゲストと本物を見分けられない。
いまの24時間・7日の数字は、ほぼ検査の痕跡。

### 何が何を呼ぶか

`npm run db:funnel` → `scripts/db-funnel.sh`（キーチェーンから接続文字列を組み立てる。既存の db:audit:prod と同じ作法）
→ `scripts/db-funnel.mjs`（`begin transaction read only` を張ってから読み、rollback で閉じる）
→ 数え方は `scripts/funnel-query.mjs` の SQL 1本。
その同じ SQL を `test/tools/run.mjs` が使い捨ての DB（PGlite）へ流し、人数が分かっている場で期待どおりかを確かめる。
本番用と試験用に別の SQL を書くと、試験が通っても本番で違う数を出すので、1本に絞ってある。

### どこまでで、どこからが未着手か

済み: 指標の定義、何が正確／近似／分からないかの一覧、本番の基準値4期間、9件の数え方の試験、
2026-09-17 にゲストが2人増えた件の読み取り監査（両方とも本番スモークの痕跡と確認）。
未着手: 閲覧・押下の記録（今回は実装しない方針）。実利用者が入るまで、率は解釈しない。

### 触ったファイル

- `scripts/funnel-query.mjs`（新規）、`scripts/db-funnel.mjs`（新規）、`scripts/db-funnel.sh`（新規）、`package.json`
- `test/tools/run.mjs`（ファネル9件）、`README.md`（道具の自己試験 37 → 46）
- `docs/funnel-v0.md`（新規）、`docs/decisions.md`（D209）、`PROGRESS.md`

### 次の一手

- 限定公開を広げて実利用者が入ったら、同じ道具で測り直して検査の痕跡と比べる

## 2026-09-18 スマホ幅のヘッダーを1段に畳んだ（D208）

### 何ができるようになったか

スマホでどのページを開いても、いちばん上の枠が1段になった。左に「つたわるかな」、右にメニューのボタン1つだけ。
行き先5つ（お題を引く・作品・ランキング・知らせ・アカウント）はボタンを押したときに、枠のすぐ下へ画面幅いっぱいの板として開く。
これまでは5つを横に並べていて、幅 536px より狭いと折り返していた（実測: 320px と 375px で3段・高さ157px、390px と 430px で2段・109px）。
その結果、320px ではトップの見出しが上から 181px の所に出ていたのが 85px になり、96px ぶん早く見えるようになった。
開け閉ては、同じボタン・Escape・メニューの外を押す・行き先を押す・別のページへ移る、のどれでも閉じる。

### 何を引き換えにしたか

狭い画面では、行き先が1回押さないと見えない（これまでは常に見えていた）。
未確認の知らせがあるときの太字も、メニューを開くまで見えない。ボタン側に印を付ければ見えるが、D112（数字も丸も出さない）に触れるので付けていない。
枠の中身がブラウザ側で動く部品になったぶん、全ページに小さな JavaScript が1つ増えた（トップは静的なまま。build の出力で確認）。

### 何が何を呼ぶか

`layout.tsx` が `SiteHeader`（`_shell.tsx`）を置き、`SiteHeader` は左の名前だけを持って、残りを `SiteNav`（`_site-nav.tsx`・新規）へ渡す。
`SiteNav` は開いているかどうかを自分で覚え、同じ `<nav>` を狭いときは板・広いときは横並びとして出す（行き先は1組しか書かない）。
「知らせ」は従来どおり `NoticeEntry`（`_notice-entry.tsx`）で、未確認の有無だけを `/api/notices/unseen` から読む。
位置と大きさの指定だけ `SiteNav` から className で渡す形に変えた（読み込み方＝D192 は1文字も変えていない）。

### どこまでで、どこからが未着手か

済み: 320/375/390/430/640/768/1280px の実測、暗い地、Escape と外側と遷移での閉じ方、ブラウザ試験3件の追加、本番反映。
広い画面は座標まで同じ（768px と 1280px で前後一致）。お題・クイズ・回答・分析・課金・DB は触っていない。
未着手: 現在のページを示す印（もともと無い）。Notion の対応表の写真の撮り直し（撮影の道具が origin/main に無い）。

### 触ったファイル

- `src/app/_site-nav.tsx`（新規）、`src/app/_shell.tsx`、`src/app/_notice-entry.tsx`
- `test/e2e/browser.mjs`（G 群に3件）、`README.md`・`docs/test-layers.md`（ブラウザ試験の件数）
- `docs/header-mobile.md`（新規）、`docs/decisions.md`（D208）、`PROGRESS.md`

### 次の一手

- 実機（iPhone / Android）で1回開いて、ボタンの押しやすさを確かめる

## 2026-09-18 トップを「引く・描く・答える」の押せる3手順に作り直した（D207）

### 何ができるようになったか

初めて来た人がトップを開くと、いちばん上に「あなたの絵、何を描いたか伝わる？」「引いて、描いて、答える。」があり、
その下に「引く」「描く」「答える」の3つの四角が横一列に並ぶ（スマホでも横一列）。四角は説明の図ではなく押せる行き先で、
引く → お題を引く画面、描く → 確定したお題を持っていればそのお題のページ（持っていなければお題を引く画面）、答える → 作品一覧。
最初に押すボタンは「お題を引く」1つだけで、答えるだけの人向けに枠だけの「描かずに、絵に答える」を横に置いた。
古い説明（「お手軽（3枚）と標準（5枚）」。今は「通常」「高難度」）も消えた。

### 何を引き換えにしたか

トップを開くたびに /api/challenge を1回余分に読む（全ページの時計の帯と同じ窓口。未サインインの人には何も作らない）。
読み終わるまでの一瞬、「描く」は /play を指している。「描いた絵で試す」はボタンから文字の入口に下げたので目立たなくなった。

### どこまでで、どこからが未着手か

済み: トップと3つの行き先の接続、320/390/1280px と暗い地での確認、ゲストでの2本の流れ（引く → お題 → 描く側 ／ 答える → 回答）、
ブラウザ試験2件の追加。お題・クイズ・回答・分析・課金・DB は触っていない。
未着手: ヘッダーの行き先がスマホで2〜3段に折り返す問題（トップの範囲外）。Notion の対応表の写真の撮り直し（撮影の道具が本流に無い）。

### 触ったファイル

- `src/app/page.tsx`、`src/app/_home-steps.tsx`（新規）
- `test/e2e/browser.mjs`（A 群に2件）、`README.md`・`docs/test-layers.md`（ブラウザ試験の件数）
- `docs/home-first-use.md`（新規）、`docs/decisions.md`（D207）、`PROGRESS.md`

### 次の一手

- 本番で初見の人に数人見てもらい、「最初に何を押したか」を聞く（数で測る道具は無い）

## 2026-09-18 履歴 20260909180000 を本番で直した（方式 C。変わったのは履歴の1行だけ）

### 何ができるようになったか

本番の migration の記録が、手元の origin/main と1行残らず一致した。20260909180000 の行は、古い課金の下書き（106文）から
実際に入っているプロフィール権限の修正（11文）の記録に置き換わった。これで、本番の履歴からファイルを作り直す操作（`migration fetch`）をしても
同じ版番号のファイルが2つにならず、その後の `db push --dry-run` も止まらない（本番へ書かずに確かめた）。
表・関数・権限・データは1つも変わっていない（部品1,515個・権限378個・public の全表の件数とハッシュが前後で同じ、構造検査は前後とも236/236）。

### 何を引き換えにしたか

本番の履歴表へ1行書き込んだ。戻すときは `node scripts/db-history-0909.mjs rollback --yes` で修復前の行へ戻せる（使い捨ての DB で確認済み、本番では使っていない）。
道具はリポジトリに残っていて、いま流すと「修復後」と判定され、`fix` は何も書かずに止まる。

### どこまでで、どこからが未着手か

済み: origin/main（de46a84）から作った作業木での事前確認（止める条件すべて不成立）、修復、直後の check・構造検査・前後の比較、fetch の経路の確認、記録。
未着手: 無し（この件）。手元の main（6d24ec1）には同じ版番号の古い課金のファイルが残っているので、その作業木から CLI の repair を流さないこと。

### 触ったファイル

- `docs/history-20260909180000.md`（冒頭に本番修復の記録）、`docs/decisions.md`（D206）、`docs/launch-checklist.md`、`docs/history-20260909180000-harness/README.md`
- `scripts/db-history-0909.mjs`（冒頭の説明に「修復済み」を足しただけ）、`PROGRESS.md`

### 次の一手

- この件は完了。次の migration を本番へ当てるときは、いつもどおり `db push --dry-run` から始める

## 2026-09-17 履歴 20260909180000 の食い違いを、使い捨ての PostgreSQL で再現し、3つの直し方を比べた（本番は読み取りだけ）

### 何ができるようになったか

本番の履歴表の食い違いが「1行の名前と文の記録だけ」で、表・関数・権限は手元のファイルどおりだと、機械的な比較で言えるようになった。
本番と同じ履歴・同じ部品・同じ権限を持つ使い捨ての DB を作り、何もしない（A）・CLI の repair（B）・1行だけの SQL（C）を同じ複製元から試した。
B と C は直した後の履歴が1文字も同じで、スキーマ・権限・データ・構造検査236は前後で変わらず、どちらも元の履歴へ1文字違わず戻せた。
後ろに migration を1本足しても3つとも正常。放置したときに壊れるのは、履歴を正本にする操作（`migration fetch` の後の `db push`、履歴だけからの作り直し）だけだった。

推奨は C。前提（作業木のファイル・履歴67行・対象行・ほかの66行・CLI の list と dry-run）を確かめてから1行だけ書き換え、違えば何も書かずに止まる道具
`scripts/db-history-0909.mjs` を作り、使い捨ての DB で直す・戻す・止まるべき5場面を通した。本番には `check`（読み取り）だけを流し、「修復前」と判定された。

### 何を引き換えにしたか

直す道具は「本番の履歴が67行」を前提にした値を持つ。次の migration が先に本番へ入ると、道具は止まるので、`docs/history-20260909180000-harness/` で値を作り直す必要がある。
`--linked` での実測と、Docker が要る `db pull` / `db diff` は測っていない。106文の下書きがどの時点のファイルだったかは git に無く、原因は推定のまま。

### どこまでで、どこからが未着手か

済み: 本番の読み取り（履歴・部品・権限・list・dry-run・構造検査236）、正常対照と本番に似せた DB、A/B/C の前後・後続 migration・戻す・再適用、
repair の正体（届いた文と前後）、fetch と履歴からの作り直し、イベントトリガーの発火、道具の通し稽古、既存の DB 試験（縦断424・アップグレード31・構造225・同時6・柵30）。
未着手: 本番で直すこと（ユーザーの判断待ち。今回は実行しない指示）。

### 触ったファイル

- `docs/history-20260909180000.md`（全面改訂）、`docs/history-20260909180000-harness/`（新規・再現の道具）、`docs/decisions.md`（D206）
- `scripts/db-history-0909.mjs`（新規）、`supabase/manual/20260909180000_history_fix.sql`・`_history_rollback.sql`（新規）
- `PROGRESS.md`

### 次の一手

- ユーザー: 本番で直すかを決める（推奨は C、急ぎではない。行うなら次の migration より前）
- 直す場合の手順・止める条件・戻し方は `docs/history-20260909180000.md` の 10 にある。→ 2026-09-17T15:27Z に実行済み（上の節）

## 2026-09-17 一般公開の前に残る非課金の問題を、本番を実際に操作して洗い直した（課金は対象外）

### 何ができるようになったか

本番を、利用者と同じようにブラウザで押して一周する確認ができるようになった（`npm run smoke:prod -- journey`）。
確認メールのリンクを別のブラウザで開く → 同意 → お題を引いて確定 → 画像つきで投稿 → 共有 → ゲストがスマホ幅で長押しして回答 →
登録者が回答・保存・通報の画面 → 作者に知らせと結果 → 削除 → サインアウト、までを本番で通し、すべて合格した。
`JOURNEY_LONG=1` を付けると、長い英数字の題名・表示名と横長の絵で同じ一周をする。

結論は「まだ一般公開できない」。利用者の流れは壊れていない。止めているのは判断と人の作業で、
一般公開への切り替え（規約の「限定公開です」の一文と検索除外）、本物の作品が2件しか無いこと、
通報の送信と Supabase の管理画面の設定（匿名サインインの上限・確認メールの本文・バックアップ）が未確認なこと。
分類の全体は `docs/launch-audit-2026-09-17.md`。

本番の smoke:work が、お題に「取引」が出るとフッターの「特定商取引法に基づく表示」を答えの漏れと数えて落ちていたので、
数える前にフッターを外すよう直した（検査の道具だけ。アプリのコードは変えていない）。

### 何を引き換えにしたか

一周の確認は本番へ書き込む（使い捨ての登録者2人・ゲスト1人・作品1件・回答2件）。作品は画面から消し、登録者は消すが、
ゲストと削除済みの作品の行、使い捨ての人の同意記録（持ち主は空）は残る。通報の送信は本物の CAPTCHA なので確かめられない。
画面のはみ出しは数字（横幅）では拾えない形があった（外側で切られる）。画面写真を見て判定する必要がある。

### どこまでで、どこからが未着手か

済み: 本番の通信の検査10本と画面の一周3回、DB の検査236項目と整合の読み取り、コードと文書の未完了の突き合わせ、5分類、test:all 16/16。
本物の作品2件と本物の利用者4人の行指紋は前後で同じ。検査用の固定利用者27人は片づけ済み（残り0）。

未着手: 本番のログ（Vercel・Supabase）は見られていない（Vercel CLI 未ログイン、管理 API の鍵に触れる操作は権限で拒否）。
A（公開阻害）は4件ともコードでは直せない（判断か人の作業）。B〜E はどれも未着手。

### 触ったファイル

- `scripts/smoke-journey.mjs`（新規）、`scripts/_smoke-http.mjs`、`scripts/run-production-smoke.mjs`
- `docs/launch-audit-2026-09-17.md`（新規）、`PROGRESS.md`

### 次の一手

- ユーザー: 一般公開に切り替えるかの判断（規約の文言は課金の作業線の規約 v2 と一緒に直す必要がある）、本物の作品を増やす、通報を1回送る、Supabase の管理画面で3つの設定を見る
- Claude（非課金）: Next.js を 16.3.3 以上へ上げる（B1）→ URL に載せた任意の文が告知の枠に出る問題（B2）→ 長い英数字でカードがはみ出す問題（B3）

## 2026-09-17 Stripe 実通信の結果を正式に反映した（D201 の返金の期待・購入の要求が重なったときの表示）

### 何ができるようになったか

- 同じ人が2つのタブから同時に購入を押しても、画面に Stripe の英語のエラーが出なくなった。
  重なったほうには「処理が重なりました。少し待ってから、もう一度お試しください。」と出て、もう一度押せば同じ決済ページへ進む。
  Stripe の元のエラーはサーバーの記録にだけ残る。二重購入を止める仕組みと、売り切れ・購入済み・販売停止の返し方は変えていない
- D201 の6段目の期待を「最終的に返金済み。途中で返金手続き中を通ることがある」に改めた（ユーザー指示）。
  権限の取り消し・枠の戻り・欠番・番号の再利用なしは、変えずに必ず確かめる
- Stripe のテストモードで、改定後の道具を最後まで通した（68項目すべて合格。重なりは受け口と画面の両方で再現して確かめた）

### 何を引き換えにしたか

重なったときの応答が 500 から 409 に変わった（合図 `CHECKOUT_BUSY`）。409 はもともと売り切れ・購入済みでも使っているので、
画面や将来の呼び出し側は、409 を合図（code）で見分ける必要がある。いまの購入ボタンは本文の文言を出すだけなので影響は無い。

### どこまでで、どこからが未着手か

済み: 改定、実装、単体2件、`local` 50・`stripe` 68、全回帰試験、push と本番 deploy、販売停止のままの本番スモーク。
次の工程の入口: `docs/history-20260909180000.md`（履歴 20260909180000 の食い違いを、使い捨ての PostgreSQL で修復手順ごと再現する。本番では repair しない）。

未着手: 想定外の失敗（`billingError` の unknown）は、これまでどおり元の文を画面へ返す。Stripe のほかの失敗の文が出る余地は残っている（今回の範囲外）。

### 触ったファイル

- `src/features/billing/conflict.ts`（新規）、`src/app/api/billing/checkout/route.ts`
- `test/unit/run.mjs`、`test/billing/stripe-e2e.mjs`
- `docs/decisions.md`（D201 の6段目と注、D205 の反映）、`docs/billing-stripe-test.md`、`docs/history-20260909180000.md`（新規）、`PROGRESS.md`

### 次の一手

- 履歴 20260909180000 を、`docs/history-20260909180000.md` の 2 の順に使い捨ての PostgreSQL で再現する（読み取りと検証だけ）
