# PROGRESS

セッションの引き継ぎを置く場所です。新しいものが上。
`/next` はここを起点に「現在地と次の一手」を出し、`/progress` がここへ追記します。
日時は JST。全体が400行を超えたら、一番下（最も古いもの）から削ります。

---
## 2026-09-12（続き） 検査作品17件の集計を直す／課金 migration の番号を整理する

### 何ができるようになったか

本番のデータベース検査が、全項目そろって合うようになった。
`npm run db:verify:keychain` は209項目すべて期待どおり（直前は205合格・4件が期待と違った）。

直したのは、持ち主のいない検査作品17件の「回答の件数」と「枠ごとの集計」。
この17件が持っていた唯一の回答は検査用330人のもので、前の作業で消えている。
回答を消しても件数の欄は減らない作りなので、1 のまま取り残されていた。
固定値を書くのではなく、`answers` を実際に数えた値（0）を書き戻し、
その作品に属する枠ごとの集計51行を消した。A8 / A21 / A24 / A32 が解消した。

課金の migration 2本は、誰も使っていない版番号へ移した（枝 `billing-renumber`）。
`20260912140000_billing_founding_creator_v0.sql` と `20260912150000_legal_v2_billing.sql`。
SQL の本文は1バイトも変えていない（改名の前後で sha256 が同じ）。

### 何を引き換えにしたか

引き換えは無し。本物の作品2件の行指紋・本物の利用者4人・公開作品2件・
回答総数324・認証と本体の整合（6項目すべて0）は、直す前と同じ。

### どこまでで、どこからが未着手か

本番の履歴 `20260909180000` の行は、名前も文も課金側のまま残っている。**今回は直さない。**
CLI は版番号だけで突き合わせるので、deploy も migration の比較も壊れていないため。
実測（CLI 2.111.0）: `migration list` は60本すべて一致・警告なし、
`db push --dry-run` は `Remote database is up to date.`。
手元の `main`（6d24ec1）は動かしていない。枝を作っただけで、取り込みは未着手。

持ち主のいない同意記録は356行ある（うち4行は 2026-09-11 の知らせ検査で増えた分）。
利用者を消しても同意記録は持ち主を空にして残す仕様なので、今回の対象にしていない。

### 何が何を呼ぶか

修復は、本番へつないだ1つのトランザクションの中で完結させた。
まず関門を通す（集計の合わない作品が固定した17IDと過不足なく一致する／17件とも
持ち主なし・削除済み・非公開・実回答0・枠3行／いいね・保存・通報・持ち主つき回答が0）。
次に `scripts/db-checks.mjs` の `checks` と `diagnostics` を**同じトランザクションの中で**
流し、直す前は4件が期待と違い、直したあとは0件になることを確かめる。
そのうえで commit する。下見（rollback）と本番（commit）で通る道は同じ。

### 触ったファイル

- `docs/decisions.md`（版番号の衝突の分類と直さない判断／重複していた D189 を D199 へ）
- `docs/launch-checklist.md`（課金の新しい版番号と、deploy が壊れていない実測）
- `PROGRESS.md`
- 枝 `billing-renumber`: `supabase/migrations/20260912140000_*.sql` と
  `20260912150000_*.sql`（改名のみ）、`PROGRESS.md`、`docs/decisions.md`

### 次の一手

- 課金の枝 `billing-renumber` を `main` へ取り込むかどうかを決める
- `20260909180000` の履歴を直す場面が来たら、そのときに手順を示してから行う

---
## 2026-09-12 公開前の本番クリーンアップ（知らせの修正を本番へ／検査用330人と検査データを削除／判定を1か所へ）

### 何ができるようになったか

未サインインでページを開いても、ヘッダーが DB に知らせの有無を聞かなくなった（本番で確認）。
デプロイ（00:55 JST、fe642c3）以降、has_unseen_results の 401 と Postgres の権限エラーは0件。
未サインインで開いた2つの窓には呼び出しが1件も無く、間を空けて開いたサインイン済みの1画面では200が1件。

2026-08 に作られた検査用の利用者330人（dpq-smoke 328・dpq-probe 2）と、その人たちの検査データを
本番から消した。作品381・お題403（カード1547・問い1209・選択肢4836）・回答355・回答の項目1065・
いいね237・保存40・通報29・枠の集計378・同意記録236 は、ID を固定したトランザクションで消した（00:43 JST）。
利用者330人は管理 API で消し（00:57 JST）、プロフィール・ドラフト403・候補6331・集計53/159・
ハンドル履歴18・認証の行が連鎖で消えた。本番の利用者は32人（本物4・メールの無いゲスト28）。
本物の作品2件と本物の利用者4人の行指紋は、前後で同じ。

検査用の利用者の見分けかたを scripts/_test-accounts.mjs の1か所にまとめた。
片づけ道具は人数を指定でき（違えば1人も消さない）、消したあとに名簿を読み直す。

### 何を引き換えにしたか

消した行は戻らない。

回答を消しても集計は減らない（既知）。持ち主のいない検査作品17件の回答件数（1のまま）と、
その作品の枠の集計51行が、この時点では実データと食い違ったまま残っていた。
db:verify:keychain は205項目が合格、4項目（A8 / A21 / A24 / A32）が期待と違った。
段A を確定する前に db:verify を下見に含めていなかったのが原因（トランザクションの外からは見えないため）。
**この4項目は同じ日の続きの作業で直した。1つ上の項目を見ること。**

片づけ道具が dpq-smoke / dpq-probe も消すようになった。作品を持つ使い捨て利用者は、
先に作品を片づけないと、作品の持ち主が空になって残る。

### 何が何を呼ぶか

`npm run smoke:prod -- fixtures --apply --expect N` → smoke-fixtures.mjs → _smoke-users.mjs の
purgeFixtureUsers が名簿を全部並べ、_test-accounts.mjs の isTestAccountEmail で対象を決める →
人数が N と違えば1人も消さずに止める → 合えば管理 API へ1人ずつ DELETE → 最後に名簿を読み直して
対象が残っていないかを数え、残っていれば失敗で終わる。cleanup-testdata.mjs も同じ判定を使う。

### どこまでで、どこからが未着手か

済み: push（dd71970 → fe642c3）、Vercel のデプロイ（成功）、本番での知らせの確認、
330人と関連データの削除、判定の統一（7153609）、全検査（型・lint・単体86・道具37・縦断344・
アップグレード31・構造198・build・ブラウザ130・スモーク11・文書15/15）。

未着手:
- 持ち主のいない検査作品17件の集計の修正（承認待ち）
- 課金2本の版番号の衝突。本番の履歴行 20260909180000 の name が billing_founding_creator_v0 で、
  実際に入った SQL と食い違う。直すには履歴の書き換えが要るので止めた（docs/decisions.md に追記）
- Vercel 側の Cron の実行履歴（CLI がサインインしていない）。Supabase の記録では 9/11 03:39 UTC の定期実行が全部 200
- 20260911120000 と D194〜D196 の番号順の扱い（2026-09-11 の質問に未回答）

### 次の一手

17作品の集計の修正を当ててよいか、ユーザーに確かめる。

### 触ったファイル

- `scripts/_test-accounts.mjs`（新規）`scripts/_smoke-users.mjs` `scripts/smoke-fixtures.mjs` `scripts/cleanup-testdata.mjs`
- `scripts/smoke-unseen.mjs`（新規。本番で知らせの呼び分けを確かめる）
- `test/tools/run.mjs`（片づけの試験）
- `README.md`（道具の自己試験 37）`docs/decisions.md`（版番号の衝突の追記）`docs/launch-checklist.md`（Cron の確かめ方）

---
## 2026-09-11 掃除の migration を本番へ当て、持ち主のいないお題96件を片づけた。知らせの問い合わせを未サインインで止めた

### 何ができるようになったか

本番の掃除が、持ち主のいない failed / discarded のお題も消し、残り件数にも数えるようになった
（22:39 JST に db:deploy で適用。履歴60本。db:verify:keychain は209項目すべて期待どおり）。
続けて掃除の関数を1回動かし、持ち主のいないお題96件を消した（22:41 JST）。内訳は
active 81・abandoned 2・failed 13。今回の migration で増えた対象は failed の13件だけで、
残り83件は前から対象だった分（次の定期掃除 12:17 JST を待たずに消えた）。
持ち主のいる failed 10件・確定済みの行・作品・回答・利用者の数、
本物の作品2件と本物の利用者4人の行指紋は、前後で変わっていない。

定期掃除の入口（/api/cron/cleanup）は使っていない。手元の .env.local の鍵は本番の鍵と違い、
401 で断られた（何も動いていない）。代わりに、入口の「お題」段が呼ぶのと同じ関数
cleanup_orphan_prompts を、同じ service_role の立場で1回呼んだ。ほかの段の残り件数は
どれも0だった。

サインインしていない人がページを開いても、ヘッダーが DB に知らせの有無を聞かなくなった。
Cookie にセッションが無ければ、その場で「無し」にする。サインイン済み（匿名のゲストを含む）は
今までどおり1回聞く。DB の権限は変えていない（検査 A44 はそのまま）。

### 何を引き換えにしたか

消した96件は戻らない。知らせを聞くかどうかは Cookie のセッションの有無で決める。
Cookie の検証と更新は同じ要求の手前で proxy.ts が済ませていて、聞いた先の判定は DB が券を
検証して行う。偽の Cookie で変わるのは「DB に聞くかどうか」だけで、見えるものは増えない。

### 何が何を呼ぶか

ヘッダー（_shell.tsx の SiteHeader）→ rpc.ts の fetchHasUnseenResults がサーバー用の Supabase
クライアントを作り、「セッションを読む口」と「DB を呼ぶ口」を unseen.ts の hasUnseenResults に渡す →
セッションが無ければ false、あれば has_unseen_results を1回呼んで、true のときだけ true を返す。

### どこまでで、どこからが未着手か

済み: migration の commit（9b451a1）と本番適用、掃除1回、知らせの修正の commit（68344c8）。型検査・lint・build・単体86・ブラウザ130・スモーク11・縦断344・道具32 すべて合格。
dpq-smoke 328人と dpq-probe 2人の監査（削除はしていない。結果は下）。
未着手: 知らせの修正は push も deploy もしていないので、本番ではまだ 401 が出る。330人の削除。

### 330人の監査（2026-09-11、本番を読むだけ）

- 330人とも example.com（予約済みで郵便が届かないドメイン）。本番は確認メールを踏まないと
  登録が済まない設定（mailer_autoconfirm = false。見えるのは今の設定だけ）なのに、330人とも
  作成から5秒以内に確認済み。メールを受け取れない以上、確認は秘密鍵で印を作れる側
  （スモークの generateSignupConfirm）がしたことになる
- 作成は 2026-08-03 04:02Z 〜 08-04 03:25Z。最後の動きも 08-04 03:25Z で、それ以後は誰も入っていない。
  直後の 08-04 12:31 JST の commit 6ff7b7e で、スモークは固定の検査用利用者へ切り替わった
- メールの形 dpq-smoke-<役>-<pid>-<乱数>@example.com は、スモークの補助（_smoke-http.mjs）だけが作る。
  役名12種のうち10種（257人）は当時のスモークの呼び出しと一致。fana / fanb（66人）と probe（3人）は
  呼び出し元の文字列を特定できていない。dpq-probe 2人は git のどこにも作成元が無い
- 持ち物: 作品381件（すべて削除済み・公開0）、お題403、ドラフト403、いいね237、保存40、回答53、通報16
- 本物の作品2件への接触0。本物の利用者4人との接触0（双方向）。保存ファイル0
- 330人の作品には、持ち主のいない回答302件と、通報者のいない通報13件が付いている

### 次の一手

- 知らせの修正の push と deploy（ユーザーの判断待ち）
- 330人を消すかどうか。消すと作品381件の持ち主が空になり（作品の外部キーは set null）、
  付いている回答302件はそのまま残る

### 触ったファイル

- `src/features/notice/unseen.ts`（新規）`src/features/notice/rpc.ts` `src/app/_shell.tsx`
- `test/unit/run.mjs`（知らせ7件）
- `README.md` `docs/launch-checklist.md`（60本すべて本番）

---
## 2026-09-11 持ち主のいない failed / discarded を掃除に含める migration を書いた（本番の6件は削除済み）

### 何ができるようになったか

本番の診断 A5（確定済みなのに作品が無いお題）が 6 → 0 になった。
消したのは、サブ指令の検査の後片づけが失敗して残った6件だけ（21:18:57 JST に確定）。
一緒に消えたのはカード19・問い19・選択肢76で、回答・作品・利用者は1行も動いていない。
本物の作品2件の行指紋は前と同じ。先に rollback で同じ処理を下見し、数が一致してから確定した。

掃除が、持ち主のいない時間切れ（failed）と自動破棄（discarded）のお題も消し、
掃除の残り件数にも数えるようにする migration を書いた。確定済み（submitted）と、
持ち主のいる行は、どの状態でも今までどおり消さない。

### 何を引き換えにしたか

6件は戻せない（検査の残骸で、回答も参照も0件だったことを確かめてから消した）。
migration を当てたあとは、持ち主のいない failed / discarded の記録は残らない。

### どこまでで、どこからが未着手か

済み: サブ指令の検査の修正を commit（e156bda）。6件の削除と事後確認。
migration と縦断試験（Q群1件で7つの形を確かめる。344件すべて合格）。
migration を外して回すと Q群だけが「持ち主のいない failed が残った」で落ちることを1回確認した。
未着手: 本番への適用と掃除（このあと preflight → 適用 → 掃除）。

has_unseen_results: サインインしていない人がページを開くと、ヘッダーが知らせの有無を
DB に聞き、anon には実行権が無いので断られる（本番の API 記録で 401 が9/11 に14回）。
画面は「無し」を出すので見た目の不具合は無い。コメントの「未サインインなら DB が false を返す」は誤り。

### 次の一手

本番の preflight（未適用がこの1本だけ・持ち主のいない failed 13 / discarded 0・持ち主のいる failed 10・
本物の作品2件）→ 適用 → db:verify:keychain → 掃除1回。

### 触ったファイル

- `supabase/migrations/20260911120000_cleanup_failed_orphan_prompts.sql`（新規）
- `test/db/run.mjs`（Q群1件）
- `README.md`（migration 60本・未適用1本・縦断344）
- `docs/launch-checklist.md`（60本のうち59本が本番）
- `docs/test-layers.md`（縦断344）

---
## 2026-09-11 サブ指令の本番検査が、自分の作った行だけを片づけるようにした（e156bda で commit）

### 何ができるようになったか

サブ指令の本番検査（smoke-sub-directive）は、終わるときに「この回に作った作品・お題・ドラフト」だけを
ID で名指しして消すようになった。1件でも消えなければ、理由と消えなかった ID を出して不合格になる。
以前は、お題を「この作者のもの全部」で消していた。同じ作者の古いお題に非公開の作品の行が残っていて、
1文まるごと外部キーに断られていた。しかもその失敗を合格と数えていた。
これが、9/10 に本番で作品の無い確定済みお題が6件残った原因。
作品は今までどおり物理的に消す（ユーザー判断。非公開化にはしない）。

### 何を引き換えにしたか

ドラフトを始めるたびに、DB へ1回余分に問い合わせる（どのドラフトができたかを読むため）。
1回の検査で最大27回増える（当たりを探すやり直し25回＋A節と J節の1回ずつ）。
道具の自己試験は、手元の DB に migration を全部当てるようになった。全体で 5.3秒（実測）。
直す前の所要時間は測っていない。

### どこまでで、どこからが未着手か

済み: 検査の修正、手元の DB での自己試験5件（9/10 の失敗の再現を含む）、道具の自己試験 32件すべて合格、lint。
未着手: 本番での検査の実行（まだ回していない）。6件の削除。時間切れ（failed）で持ち主のいない13件への対応。
commit もまだ。検査が途中で例外で止まると、後片づけまで届かない点は前と同じ。

### 何が何を呼ぶか

検査は、ドラフトを始めた直後・お題を確定した直後・作品を出した直後に、できた ID を帳面
（_smoke-own-rows.mjs の createRunLedger）に積む。後片づけでは cleanupRunRows が帳面を
作品 → お題 → ドラフトの順に読み、表ごとに1回「この ID の行を消せ」と頼む。消えた ID の一覧を受け取り、
頼んだ一覧と突き合わせる。その結果を must へ渡す。回答は作品と一緒に消えるので、回答の ID が
残っていないことを別に確かめる。

### 次の一手

6件の削除 SQL を、下見（rollback）→ 承認 → 本番の順で実行する。ユーザーの指示文が
「1. sub-directive スモークを修正」の途中で切れて届いたので、2 以降は指示を待つ。

### 触ったファイル

- `scripts/_smoke-own-rows.mjs`（新規。帳面と、名指しで消す処理）
- `scripts/smoke-sub-directive.mjs`（ID を積む。後片づけを名指しに）
- `test/tools/run.mjs`（自己試験5件）
- `README.md`（道具の自己試験 27 → 32項目）

---
## 2026-09-11 修正を本番へ当て、検査用の利用者119人を正規の経路で片づけた

### 何ができるようになったか

管理API（正規の削除経路）で、引きかけのお題を持つ利用者も消せるようになった。
それを使って、本番に残っていた検査用の利用者119人を消した。119人とも HTTP 200、
失敗0（Auth ログで 119件・すべて検査用・エラーなしを確認）。SQL で直に消してはいない。

- 07:59:41Z〜07:59:52Z  `npm run db:deploy` で migration 1本を適用（履歴 58→59）
- 08:00:47Z〜08:01:11Z  `npm run smoke:prod -- fixtures --apply`（直前の下見で119人を確認してから）

### 何を引き換えにしたか

本物の利用者・本物の作品には何も起きていない（本物の作品2件の行指紋が前後で同じ）。
ただし、消した119人が持っていたお題は持ち主が外れた（外部キーの SET NULL。設計どおり）。
持ち主のいないお題が、進行中 0→81・破棄 0→2・時間切れ 0→13 に増えた。
進行中と破棄の83件は、日次の掃除（cleanup_orphan_prompts）が次の実行で消す。
時間切れの13件は掃除の対象外なので残る。確定済みで作品の無い6件は変わらず6件。

### どこまでで、どこからが未着手か

済み: 適用・片づけ・前後の突き合わせ。本番の DB 検査は 208 合格・不合格1
（A5 確定済みで作品の無いお題6件。当てる前から同じ。今回は触らない指示）。
未着手: 6件のお題の手当て、`dpq-smoke-…@example.com` 328人と `dpq-probe-…@example.com` 2人の扱い、
時間切れの持ち主なしお題13件の扱い。いずれも指示待ち。push はしていない。

### 前後の数

利用者 481→362（検査用 119→0 / example.com 330 / ゲスト 28 / 本物 4 は不変）
profiles 481→362 / draft_candidates 17904→6840 / 同意の記録 592（持ち主が外れたもの 352）
関数185・表63・SECURITY DEFINER 167→168・資格を切り替えない AFTER トリガー 1→0
残骸（auth に居ないのに public に残る行）0

### 触ったファイル

- PROGRESS.md（この項目）
- docs/launch-checklist.md（手順 -1 の現状を「59本すべて適用済み」に直した）
---
## 2026-09-11 利用者を消せない不具合の修正を commit し、本番へ当てる直前まで確かめた

### 何ができるようになったか

修正一式を 313cd96 として commit した（まだ本番へは当てていない、push もしていない）。
当てる直前に確かめるべきことを、本番を読むだけで全部確かめた。

- 接続先は oyyiutiptsffckujqkvl で一致。本番の未適用は新しい1本だけ（58本は一致）
- migration の SHA256 は作業木と commit で一致（97fffc40…22fcc1）
- 本番の関数はいま prosecdef=false・持ち主 postgres・search_path 空。
  接続する役も postgres なので、ALTER FUNCTION を実行できる
- 同じ名前の関数は1つだけ（取り違えない）。長い取引は0件（当てるときに待たされない）
- 関数本体の指紋が本番と手元で一致（fb82d9df…）。当てたあとも変わらないことを
  PostgreSQL 17.5 で確かめた。二度当てても落ちない
- 片づけの下見: 利用者481人のうち消す119人・残す362人（前回から増えた4人はゲスト）

### 何を引き換えにしたか

引き換えは無し。本番には1バイトも書いていない。

### どこまでで、どこからが未着手か

済み: commit、本番の読み取りによる直前確認、PostgreSQL 17.5 での前後比較。
未着手: 本番への適用（承認待ち）と、119人の片づけ。

ブラウザ試験は 313cd96 の上で 130/130 合格（下ごしらえ最遅 1162ms、load 3〜5）。
前回の 128/130 は機械の詰まり（swap 満杯・load 15）による読み込み待ちの時間切れだった。

### 次の一手

承認をいただいたら npm run db:deploy → npm run db:verify:keychain →
npm run smoke:prod -- fixtures（下見）→ --apply → 下見で0人を確認。
当てる前の基準値: 履歴58本・関数185・表63・SECURITY DEFINER 167本・
本物の作品2件の行指紋 f474f359… / 8178ffac…

### 触ったファイル

- PROGRESS.md（この項目）
