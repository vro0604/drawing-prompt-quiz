# PROGRESS

セッションの引き継ぎを置く場所です。新しいものが上。
`/next` はここを起点に「現在地と次の一手」を出し、`/progress` がここへ追記します。
日時は JST。全体が400行を超えたら、一番下（最も古いもの）から削ります。

---
## 2026-09-12 D194〜D196 の版番号を 20260912110000 / 120000 / 130000 へ振り直した（D198。本番は未実行）

### 何ができるようになったか

D194〜D196 の3本が、本番の最大（20260911120000、別の作業線が当てた掃除）より新しい番号になった。
本番へ1本当てるごとに、履歴の最大がその版になる（61本 → 62本 → 63本）。
当てた順と版の順が一致し、古い番号を後から当てる特別な扱い（`--include-all`）が要らなくなった。
使い捨ての PostgreSQL 17.6 で、本番と同じ60本のあとに新しい3本を1本ずつ当てて確かめた。

### 何を引き換えにしたか

SQL の本文は変えていない（sha256 が旧版と同じ）。そのため SQL の中のコメントには古い番号が残る
（各ファイル1行目のファイル名、D195・D196 の中の「Phase 1（20260911090000）」など）。

### 分かったこと

- origin/main が 6f4521a（別の作業線）まで進んだ。案Aの「p0-p5 を main へ」は済んでいる。残りは onboarding の merge だけ
- 本番の Vercel には fe642c3 が deploy され、検査用の利用者330人が消えた（回答 679 → 324）。
  別の作業線の記録では db:verify:keychain の4項目（A8 / A21 / A24 / A32）が期待と違う（D70 の既知の問題）
- 6f4521a と merge すると、文書が5つ衝突する（docs/decisions.md が増えた）。コードの衝突は0

### 次の一手（どれも人の承認が要る）

1. onboarding-phase12-integration を main（6f4521a）へ merge し、文書5つの衝突を解く
2. docs/landing-d194-d196.md の手順で、clean checkout から3本を1本ずつ当てる

### 触ったファイル

- supabase/migrations/2026091{2110000,2120000,2130000}_*.sql（名前だけ変更）
- supabase/rollback/2026091{2110000,2120000,2130000}_*_rollback.sql（名前と中の版番号の文字）、supabase/rollback/README.md
- test/db/run.mjs（互換 migration の境目 COMPAT_FROM）、scripts/landing-d194-expected.json
- docs/landing-d194-d196.md、docs/decisions.md（D198。D195・D196・D197 に振り直しを書き足した）、PROGRESS.md

---
## 2026-09-12 D194〜D196 を本番へ出す準備（取り消し SQL・停止条件の道具・手順書。本番は未実行）

### 何ができるようになったか

D194〜D196 の3本を本番へ1本ずつ当てるとき、1本ごとに「止まるべきか」を機械で判定できるようになった。
判定の道具は本番を読むだけで、DB へは何も書かない。当てる前の判定（pre）を本番で1回流し、
2253個の数字の指紋を失敗なしで読めた。当てる3本が触る部品は、使い捨ての DB と定義も権限も同じだった。

失敗したときに戻す SQL を3本用意した。システム回答が0件・待ち行列が0行のときだけ動き、
D196 → D195 → D194 の順でしか動かない。どれもまだ本番へ流していない。

本番と同じ60本・同じ文字コード・同じ既定の権限を持つ使い捨ての PostgreSQL 17.6 で、
「1本当てる → 履歴へ記録 → 判定」を3回と、取り消し3回を通した（63項目すべて ○）。

### 何を引き換えにしたか

- 期待値（scripts/landing-d194-expected.json）を作った再現の道具は repo の外（スクラッチパッド）にある。
  作り直すにはその道具が要る
- 当てるのは「着地後の main の clean checkout から」に限った。手元の作業木からは打てない

### 分かったこと

- 20260911120000 は別の作業線がすでに commit し（9b451a1）、2026-09-11 22:39 JST に本番へ当てた。
  本番は60本・最大 20260911120000。D194 は本番の最大より古い番号で当てることになる（再現して通した）
- p0-p5-production-integration は fe642c3 まで進み、アプリの変更（68344c8 知らせの修正）も入った
- merge すると文書4つ（PROGRESS / README / launch-checklist / test-layers）が衝突する。コードは衝突0。
  合わさった木で縦断384・道具32・単体86・柵27・アップグレード31・構造211・型・lint・build・ブラウザ130 すべて合格
- repair は SQL 本体を流さず、db push と同じ形で履歴を書く。ただし打った場所のファイルを黙って信じる
  （書き換えた・別内容・同じ版が2つ、のどれもエラーにならない）

### 次の一手（どれも人の承認が要る）

1. D194 をこのままの番号で当てるか（再現で通した）、付け替えるかを決める
2. p0-p5-production-integration の持ち主が main を fe642c3 まで進める
3. onboarding-phase12-integration を merge し、文書4つの衝突を解く
4. docs/landing-d194-d196.md の手順で、clean checkout から3本を1本ずつ当てる

### 触ったファイル

- docs/landing-d194-d196.md（新規。手順書）
- scripts/landing-d194-check.mjs / scripts/landing-d194-expected.json（新規。停止条件の判定と期待値）
- supabase/rollback/2026091{1090000,2090000,2100000}_*_rollback.sql（新規）、supabase/rollback/README.md
- docs/decisions.md（D197）
- PROGRESS.md（この節。400行に収めるため、いちばん古い「本番向けの検査を、新しい画面に追いつかせた」を削った）

---
## 2026-09-11 Phase 1・2 を本番相当の土台へ載せ直し、システム回答を分析と取り込み枠から外した（D196。本番未適用）

### 何ができるようになったか

Phase 1（回答の出所）と Phase 2（システム回答の待ち行列）が、**本番と同じ59本の
migration の上で**動くようになった。これまでは本番より古い土台（profile-on-main）の
上でしか試していなかった。

載せ直して分かった穴も塞いだ。本番にだけあった作者向けの分析と取り込み枠は、
回答の出所を知らなかったので、システム回答が1件入ると作者の枠を1つ使い、
分析にも1人分として混ざるはずだった。いまは、システム回答は分析にも枠にも入らない。
人間の回答だけの作品では、何も変わらない（前後で読める数字が1つも変わらないことを試験で確かめた）。

### 何を引き換えにしたか

- 作者の回答一覧と「分析から外す」の通し番号は、人間の回答だけに振る。
  システム回答は番号を持たないので、作者は外す操作でそれを指定できない
  （存在しない番号として今までどおり断る）
- 運営の利用状況（get_usage_summary）の回答数は、まだシステム回答も数える。
  今回の範囲（分析と取り込み枠）の外なので触っていない

### 何が何を呼び、値がどこへ渡るか

回答が answers に1行入ると、引き金 answers_after_insert_auto_import が走る。
ここで出所が human でなければ、そのまま帰る（枠を読みにも行かない）。
human なら consume_import_capacity を呼び、そこでも human の回答だけを古い順に選んで
analysis_imports へ入れる。作者の集計・掘り下げ・語ごとの数は、母集団を返す
analysis_all_answers / analysis_advanced_answers を通して回答を受け取り、
その2本が human だけを返す。回答一覧・外す操作・枠の状態・答えた本人の集計は
answers を直接読むので、それぞれの読み取りに同じ条件を足した。

### どこまでで、どこからが未着手か

- 済み: 新しい作業線 onboarding-phase12-integration（8a8a947 から）、Phase 系6コミットの載せ替え、
  互換 migration 20260912100000、SC 群7件、D196、spec §21
- 確かめたこと（2026-09-11）: 縦断試験 383/383、DB構造 211/211、アップグレード 31/31、
  ブラウザ全件 130/130（中央値 3.4秒・全体 10.9分・開始時の交換領域 9.0GB）、型検査・lint・build 終了コード0。
  PostgreSQL 17.6 でも縦断試験 380/383（落ちた3件は語彙の候補の検査で、土台の 8a8a947 でも同じ3件が落ちる）
- 未着手: 本番への適用（D194・D195・D196 の3本）、get_usage_summary の扱い、
  回答の知らせの文言、本番履歴 20260909180000 の修復（別作業）

### 次の一手

1. この作業線を本番へ出すかを人が決める（出すなら 3本を db:apply:one で順に当て、repair で記録）
2. get_usage_summary を人間に絞るかを決める
3. 20260909180000 の偽の履歴（課金の下書き）の修復を、課金の作業線と合わせて決める

### 触ったファイル

- supabase/migrations/20260912100000_system_answer_out_of_analysis_and_import.sql（新規）
- test/db/run.mjs（SC 群）、scripts/db-checks.mjs（A62）
- docs/decisions.md（D196）、docs/spec.md（§21）、README.md・docs/launch-checklist.md・docs/test-layers.md（件数）
- PROGRESS.md（この節。400行に収めるため下の古い5節を削った。Phase 1 の節の中身は D194 と、この節にある）

---
## 2026-09-10 システム回答を作る道を、サーバー専用で用意した（D195。Phase 2。本番未適用）

### 何ができるようになったか

仕組みが作品に1件だけ答えるための道が、データベースの中にできた。
積む・取り出す・保存する・失敗を記録する・取り消す・もう一度試せるようにする。

**まだ何も自動では動かない。**作品を投稿しても待ち行列へは積まれず、
外部の AI にもつながっていない。いま通せるのは検査だけ。

### 何を引き換えにしたか

引き換えは無し。既存の関数は1本も差し替えていない。表を1つ足し、
関数を7本足しただけ。人が回答を出す窓口も、作品も、お題も1文字も変えていない。

### 何が何を呼び、値がどこへ渡るか

信頼されたサーバーが「この作品を積んで」と頼むと、待ち行列に1行できる。
同じ作品へ何度頼んでも行は1つのまま。

働き手が「仕事をください」と頼むと、待っている行が1つ渡され、その行は処理中に
なる。渡すときにその行を押さえるので、別の働き手には見えない。だから同じ仕事が
2人に渡ることはない。

保存を頼むときに渡すのは「どの問に、どの語を選んだか」だけ。
**合っているかどうかは渡さない。**保存する側が選択肢の表を見て自分で決める。

保存する直前に、作品が消えていないか・公開されているか・表示を止められて
いないか・持ち込みではないか・人が先に答えていないか・すでにシステム回答が
無いか・問がその作品のものか・語がその問の選択肢にあるか・数と重複、を
全部見直す。1つでも合わなければ1行も書かない。

### 決めたこと

- システム回答の持ち主は空（NULL）。実測で answers.user_id はもともと空を許す列
- prompt_first と art_first は prompts.origin で分かれる。新しい列は作っていない
- 作品を本当に消すと待ち行列の行も一緒に消える。だから**この表を
  「初回体験が済んだ印」として使わない**（作品を1つ消すと初心者へ戻ってしまう）

### 検査

縦断試験 294/294（SQ群21件を追加）／ DB構造の検査 209/209（A56〜A61を追加）／
型検査0 ／ lint エラー0・警告2（既存）／ アップグレード試験 29/30（既存の1件）／
柵 30/30 ／ 語彙564語一致 ／ 配色 ／ build 成功 ／
スモーク 11/11（3回目。1・2回目は smoke-race が落ちたが、同じ本は 09-08 と
09-09 にも落ちており私の変更より前から時々落ちる）／
ブラウザ全件 92/92（中央値2.9秒・6.6分。開始前の交換領域15.9GB）／
check:docs 18件の食い違い（既存。指示24により混ぜて直さない）

### 触ったファイル

- `supabase/migrations/20260912090000_system_answer_queue.sql`（新規）
- `src/features/system-answer/types.ts`（新規）
- `src/features/system-answer/rpc.ts`（新規。まだ誰も呼んでいない）
- `test/db/run.mjs`（SQ群21件）
- `scripts/db-checks.mjs`（A56〜A61。表の数を52→53へ）
- `docs/decisions.md`（D195）／ `docs/spec.md`（20節）／ `PROGRESS.md`

### 次の一手

Phase 3。未決は Phase 3 の節に書いた通りで、まず正式名称（画面に出す言葉）と、
初回体験の進み具合をどこに持つかの2つが要る。

---
## 2026-09-10 ブラウザ試験が緑にならない原因を、端末の交換領域と特定した

### 何ができるようになったか

ブラウザ全件試験を走らせると、**始める前の端末の混み具合が記録に残る。**
交換領域（swap。記憶領域が足りないとき中身を保存装置へ追い出す仕組み）の
使用量、平均待ち行列、同時に走っている `next-server` / ブラウザ / 試験の数。
終わった後にもう一度測るので、詰まっていたのが最初からなのか、
この試験自身が押し出したのかを後から見分けられる。

1件あたりの所要時間の中央値・90%点・最長も、**合格した試験を混ぜて**
記録に入る。混ぜるから意味がある。端末が詰まっている回は、落ちた試験だけで
なく通った試験まで一様に伸びる。落ちた試験だけを見ると、その差が見えない。

### 何を引き換えにしたか

引き換えは無し。試験の待ち時間・判定・件数は1つも変えていない。
測るのに増えた時間は、始める前の1回と終わった後の1回で、
どちらも `sysctl` と `ps` を1回ずつ呼ぶだけ。

### 何が何を呼び、値がどこへ渡るか

`startApp` が、ポートを押さえるより前に `readMachine()` を呼ぶ。
返った値は画面へ1行出したうえで、立ち上げたアプリの持ち物として返る。
`browser.mjs` はそれを最後に `recorder.finish()` へ渡し、
そのとき自分でもう一度測って「終了後」として並べる。
中央値・90%点・最長は `finish()` が全件の所要時間から自分で出す。

### 今回分かったこと

全件92件が6回続けて緑にならなかった（90/92, 90/92, 起動失敗, 91/92,
60/92, 81/92）。原因は交換領域の枯渇。この端末は記憶領域 8GB に対し、
試験を始めた時点で交換領域を 13.0GB 使っていた。
1件あたりの中央値が 3,215ms → 24,217ms へ8倍に伸びており、
**合格した試験まで一様に遅くなっていた。**

他の作業線の試験・組み立てが止まっている状態で流し直したら、
92/92 が2回続けて出た（6.3分・中央値3,215ms ／ 5.8分・中央値2,925ms）。

### 触ったファイル

- `test/e2e/machine.mjs`（新規。交換領域・平均待ち・重いプロセスを測る）
- `test/e2e/server.mjs`（`startApp` の先頭で測って返す）
- `test/e2e/record.mjs`（中央値・90%点・最長と、端末の2点を記録へ）
- `test/e2e/browser.mjs`（終了時に測って `finish()` へ渡す）
- `docs/test-layers.md`（層0へ「端末のせいかコードのせいかを分ける」を追記）

### 次の一手

Phase 2（システム回答を書く窓口と待ち行列）。未決は2つだけ先に要る。
正式名称（画面に出す言葉）と、対象を prompt_first だけにするか art_first も
含めるか。

**他の作業線のプロセスと編集機の拡張は1つも止めない。**
出所: ユーザー指示（2026-09-10）。

---
## 2026-09-11 修正を本番へ当て、検査用の利用者119人を正規の経路で片づけた

### 何ができるようになったか

管理API（正規の削除経路）で、引きかけのお題を持つ利用者も消せるようになった。
それを使って、本番に残っていた検査用の利用者119人を消した。119人とも HTTP 200、
失敗0（Auth ログで 119件・すべて検査用・エラーなしを確認）。SQL で直に消してはいない。

- 07:59:41Z〜07:59:52Z  `npm run db:deploy` で migration 1本を適用（履歴 58→59）
- 08:00:47Z〜08:01:11Z  `npm run smoke:prod -- fixtures --apply`（直前の下見で119人を確認してから）

### 何を引き換えにしたか

本物の利用者・本物の作品には何も起きていない（本物の作品2件の行指紋が前後で同じ）。
ただし、消した119人が持っていたお題は持ち主が外れた（外部キーの SET NULL。設計どおり）。
持ち主のいないお題が、進行中 0→81・破棄 0→2・時間切れ 0→13 に増えた。
進行中と破棄の83件は、日次の掃除（cleanup_orphan_prompts）が次の実行で消す。
時間切れの13件は掃除の対象外なので残る。確定済みで作品の無い6件は変わらず6件。

### どこまでで、どこからが未着手か

済み: 適用・片づけ・前後の突き合わせ。本番の DB 検査は 208 合格・不合格1
（A5 確定済みで作品の無いお題6件。当てる前から同じ。今回は触らない指示）。
未着手: 6件のお題の手当て、`dpq-smoke-…@example.com` 328人と `dpq-probe-…@example.com` 2人の扱い、
時間切れの持ち主なしお題13件の扱い。いずれも指示待ち。push はしていない。

### 前後の数

利用者 481→362（検査用 119→0 / example.com 330 / ゲスト 28 / 本物 4 は不変）
profiles 481→362 / draft_candidates 17904→6840 / 同意の記録 592（持ち主が外れたもの 352）
関数185・表63・SECURITY DEFINER 167→168・資格を切り替えない AFTER トリガー 1→0
残骸（auth に居ないのに public に残る行）0

### 触ったファイル

- PROGRESS.md（この項目）
- docs/launch-checklist.md（手順 -1 の現状を「59本すべて適用済み」に直した）
---
## 2026-09-11 利用者を消せない不具合の修正を commit し、本番へ当てる直前まで確かめた

### 何ができるようになったか

修正一式を 313cd96 として commit した（まだ本番へは当てていない、push もしていない）。
当てる直前に確かめるべきことを、本番を読むだけで全部確かめた。

- 接続先は oyyiutiptsffckujqkvl で一致。本番の未適用は新しい1本だけ（58本は一致）
- migration の SHA256 は作業木と commit で一致（97fffc40…22fcc1）
- 本番の関数はいま prosecdef=false・持ち主 postgres・search_path 空。
  接続する役も postgres なので、ALTER FUNCTION を実行できる
- 同じ名前の関数は1つだけ（取り違えない）。長い取引は0件（当てるときに待たされない）
- 関数本体の指紋が本番と手元で一致（fb82d9df…）。当てたあとも変わらないことを
  PostgreSQL 17.5 で確かめた。二度当てても落ちない
- 片づけの下見: 利用者481人のうち消す119人・残す362人（前回から増えた4人はゲスト）

### 何を引き換えにしたか

引き換えは無し。本番には1バイトも書いていない。

### どこまでで、どこからが未着手か

済み: commit、本番の読み取りによる直前確認、PostgreSQL 17.5 での前後比較。
未着手: 本番への適用（承認待ち）と、119人の片づけ。

ブラウザ試験は 313cd96 の上で 130/130 合格（下ごしらえ最遅 1162ms、load 3〜5）。
前回の 128/130 は機械の詰まり（swap 満杯・load 15）による読み込み待ちの時間切れだった。

### 次の一手

承認をいただいたら npm run db:deploy → npm run db:verify:keychain →
npm run smoke:prod -- fixtures（下見）→ --apply → 下見で0人を確認。
当てる前の基準値: 履歴58本・関数185・表63・SECURITY DEFINER 167本・
本物の作品2件の行指紋 f474f359… / 8178ffac…

### 触ったファイル

- PROGRESS.md（この項目）
---
## 2026-09-10 利用者を正規の経路で消せなかった原因を突き止めた

### 何ができるようになったか

本番の検査用利用者119人が、Supabase の管理API（正規の削除経路）で
必ず 500 になっていた。その原因が分かり、直し方も手元の PostgreSQL 17 で
実際に確かめた。**まだ本番へは当てていない（承認待ち）。**

原因は、引きかけのお題の「最後にさわった時刻」を書き直す引き金1つ。
利用者を消すと外部キーの後始末が
auth.users → profiles → draft_sessions → draft_candidates と連鎖する。
Postgres はこの後始末を「消される表の持ち主」の資格で走らせるが、
AFTER 行トリガーだけは文の終わりにまとめて実行されるため、そのときには
資格が呼び出し元（認証サービス）に戻っている。認証サービスは public の表に
権限を1つも持たないので、そこで draft_sessions を書こうとして断られる。

本番ログの実測（2026-09-10T14:36:46Z）:
`permission denied for table draft_sessions (SQLSTATE 42501)`。
消せた198人は draft_candidates を持っていなかった。残る119人は全員持っている。

### 何を引き換えにしたか

引き換えは無し。関数の中身は1行も変えず、実行資格だけを持ち主のものにする。
アプリの通常の経路は SECURITY DEFINER の RPC か service_role で動いていて、
anon と authenticated はこの2表に権限を1つも持たない（実測）ので、
見え方も動きも変わらない。

同じ形の見落としを二度としないために、DB構造の検査へ1項目足した。
「削除・更新で走る AFTER トリガーに、資格を切り替えないものが0本」。
いまの本番に当てると、この1本だけが引っかかる。

### どこまでで、どこからが未着手か

済み: 原因特定、migration の作成、PostgreSQL 17.5 での再現と検証、
DB構造の検査への項目追加、片づけ道具の失敗理由の出し方の作り直しと自己試験6件。
未着手: 本番への適用（承認待ち）と、そのあとの119人の片づけ。

### 次の一手

1. `20260910210000_touch_session_as_owner.sql` を本番へ当てる（`npm run db:deploy`。
   本番の最後より新しい版番号なので、履歴も自動で入る。repair は要らない）
2. `npm run db:verify:keychain` で新項目を含めて確認
3. `npm run smoke:prod -- fixtures`（下見）→ `--apply`（実行）→ 下見で0を確認

### 触ったファイル

- supabase/migrations/20260910210000_touch_session_as_owner.sql（新規）
- scripts/db-checks.mjs（AFTER トリガーの資格を見る項目を追加）
- scripts/_smoke-users.mjs（生のまま叩いて理由を読む／理由を種類ごとに数える／試験から差し替えられる口）
- scripts/smoke-fixtures.mjs（理由の出し方）
- test/tools/run.mjs（片づけ道具の自己試験6件）
- README.md / docs/test-layers.md / docs/launch-checklist.md（件数と、手順 -1 の現状）

