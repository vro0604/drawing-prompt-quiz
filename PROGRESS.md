# PROGRESS

セッションの引き継ぎを置く場所です。新しいものが上。
`/next` はここを起点に「現在地と次の一手」を出し、`/progress` がここへ追記します。
日時は JST。全体が400行を超えたら、一番下（最も古いもの）から削ります。

---

## 2026-08-27 13:16

### 終えたこと

- **このファイル自体が、今回いちばん大きな発見だった。** CLAUDE.md も
  `.claude/commands/` の5つも PROGRESS.md がある前提で書かれているのに、
  リポジトリには一度も存在していなかった（git log で確認）。
  `/next` は「無ければ聞いて止まる」、`/progress` は「無ければ新規作成する」で
  食い違っていて、無人実行はそのままだと毎回止まる。/progress 側に従って作った。
  この置き場所でよいかは docs/PENDING.md の 8 に出してある。
- PR #1 の Vercel プレビューが失敗した。切り分けた結果、**この PR の差分が
  原因ではない。** /privacy と /terms が規約本文を DB から読んでいて、
  `next build` がページデータを集める段でそれを一度実行する。そこで
  Supabase の環境変数が無いと `assertSupabaseEnv()` が投げてビルドが止まる。
  origin/main の木を環境変数なしでビルドしても同じ行で落ち、ダミーの値を
  入れれば PR のブランチも main も通ることを実測した。プレビュー環境に
  環境変数が入っていないだけで、PR が1本も無かったので今まで踏まなかった。
  BACKLOG.md の B-005 と docs/PENDING.md の 9 に置いた。
  **Vercel のログを読む手段（VERCEL_TOKEN）がこの環境に無い**ので、
  デプロイの再実行もできない。
- docs/PENDING.md を新規作成し、答えの出ていない判断を9件並べた。
  中心は D157 の見た目の分岐で、**「紙・掲示板寄りにするか」だけが未確認のまま、
  残り4つがその前提で積まれている**という形になっている。ここが崩れると4つとも
  組み直しになるので、1件目に置いた。
- BACKLOG.md を新規作成し、雑務でないと仕分けた5件を積んだ。
- 雑務を4件直した。README の決定ログの件数が 144 のままだった（実際は 157）。
  .env.example の CAPTCHA の説明が実装と逆（「秘密鍵が未設定だと素通しする」と
  書いてあるが、本番では通報自体を 503 で止める）。コードが読んでいる
  TURNSTILE_EXPECTED_HOSTNAME が見本に無かった。/health の注記が
  「Step 3 でテーブルを作成したら置き換えます」のまま（Step 17 まで完了している）。
- layout.tsx が `NEXT_PUBLIC_SITE_URL ?? "http://localhost:3000"` を自前で持って
  いたのを、src/lib/env.ts の SITE_URL を使う形に寄せた。既定値が2か所にあり、
  末尾スラッシュを落とす処理は片方にしか無かった。
- 自己監査の食い違い: docs/QUEUE.md は空だったので自律モードに入った。
  ロードマップの実測（◯6 / ?3 / ✗2）と、コード側の状態（typecheck・lint・
  check:contrast がすべて通る）は矛盾していない。**残っている ✗ は2つとも
  外部サービスの操作で、コードの側にやることは無い。**

### 次の一手

**docs/PENDING.md の 9（プレビューのデプロイをどう通すか）と
1（紙・掲示板寄りにするか）の2つ。**

9 は PR #1 が赤いままなので先に要る。私の推奨は (b)、つまり /privacy と
/terms を `force-dynamic` にしてビルド中に DB を読まない形にすること。
答えが出れば次回のルーチンで直せる。(a)（Vercel に環境変数を入れる）を
選んだ場合は、Vercel の画面での操作になるのでルーチンは手を出せない。

1 は見た目の作業すべての入口。ここが決まらないと 2・3・4 と
BACKLOG の B-003・B-004 が全部動かせない。9件のうち5件がこの1件に
ぶら下がっている。

そのあとは公開手順5（カスタムSMTP）。PENDING の 5 で、Gmail の SMTP と
Resend のどちらで進めるかを決める必要がある。roadmap.md の中で
83行目と 188行目が違う前提で書かれたまま残っている。

BACKLOG.md に4件積んだ。着手できる状態で書いてある。

docs/QUEUE.md の未着手は空のままにした。今回やり残した作業のうち、
**判断を待たずに手を動かせるものが1つも無かった**ため。書き足すと、
次のルーチンが答えの無いまま実行してしまう。

### 触ったファイル

- PROGRESS.md（新規）
- BACKLOG.md（新規）
- docs/PENDING.md（新規）
- README.md
- .env.example
- src/app/layout.tsx
- src/app/health/page.tsx
